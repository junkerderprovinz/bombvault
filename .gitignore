# Dependencies
node_modules/

# Next.js / build
.next/
out/
dist/
build/
tsconfig.tsbuildinfo

# Keep the embedded-SPA placeholder so `//go:embed all:web/dist` compiles
# before the Vite build exists; real build artifacts under web/dist stay ignored.
!web/dist/
web/dist/*
!web/dist/index.html

# Env & secrets
.env
.env.*
!.env.example

# Local data
*.sqlite
*.sqlite-*
/data/
/config/

# Brainstorm / tooling
.superpowers/

# OS / editor
.DS_Store
Thumbs.db
*conflicted copy*

---
phase: 03-selection-trust-controls
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - web/src/pages/Containers.tsx
  - web/src/components/SelectionTree.tsx
  - web/src/lib/api.ts
  - web/src/lib/i18n.ts
  - web/src/lib/locales/*.ts
  - web/src/pages/Containers.tree.dom.test.tsx
  - web/dist
autonomous: true
requirements: [INTEG-04, SELECT-03, RESTIC-01]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Clicking Reset selection (after the fail-tone confirm naming both consequences) sends PATCH backupPaths: [] whose body carries NO selectionSource field, through the same one-deep serialized queue as toggles (D-05)"
    - "A successful reset refetches mounts and renders the auto-detected appdata-default selection with remembered exclusions gone; lastSavedCount re-initializes from the refetch"
    - "The zero-include block message (folders.emptySelectionBlocked) references the Reset selection control, and folders.hint no longer claims that unticking everything reverts to the automatic default"
    - "After a successful backupPaths save whose attempted include count is lower than the last-saved count, and only when container.lastBackup is not null, a role=status text-statusWarn narrowing note renders under the tree; it never fires on a net-widening save, when lastBackup is null, or for the reset save"
    - "The narrowing note is transient editor-session state, absent on reopen (no server-side include-count history exists), and never compares snapshot contents (SELECT-06 stays deferred)"
    - "Every root row carries a 'Skip cache folders (CACHEDIR.TAG)' switch with an InfoBubble tooltip disclosing item-wide scope; flipping it PATCHes the full excludeCaches map and never runs concurrently with a backupPaths PATCH from the editor (one serialized stream, one fetch per drain)"
    - "A failed toggle or reset PATCH shows the verbatim server error in a fail toast, shakes the triggering control, and reverts to the stored value; the reset never partially mutates selection state (non-optimistic)"
    - "An in-flight PATCH disables the switch or button with no spinner and no layout shift"
    - "The switch renders disabled on unreachable mounts and also renders on custom roots (same map, same union)"
    - "InfoBubble text wraps within its own 280px max-width chrome; the confirm message, the toggle label and the narrowing note wrap as inline text; user-facing copy is never truncated"
    - "All 7 new keys plus 2 text changes exist across all 42 locale tables with placeholders intact and no em dashes; parity, quality and orphans tests pass"
  artifacts:
    - "web/src/pages/Containers.tsx: SaveDesc gains a per-attempt selection source and reset kind; attemptSave composes the body per the latest pending descriptor; lastSavedCount ref and narrowing-note state; lastBackup prop threaded from ContainerRow; reset Button with useConfirm and refetch-on-ok; excludeCaches map state and serialized toggle PATCH path"
    - "web/src/components/SelectionTree.tsx: per-root CACHEDIR toggle sub-row (Toggle with hideLabel, caller-drawn label, InfoBubble scope tooltip)"
    - "web/src/lib/api.ts: ContainerMountsResponse.excludeCaches field and the single composed-body PATCH helper the queue uses"
    - "web/src/lib/i18n.ts plus 40 locale files: keys folders.narrowedNote, folders.resetSelection, folders.resetConfirm, folders.cachedirToggle, folders.cachedirScope and text changes to folders.emptySelectionBlocked and folders.hint"
    - "web/src/pages/Containers.tree.dom.test.tsx: reset, narrowing and toggle contract cases"
    - "web/dist rebuilt and committed (phase gate)"
  key_links:
    - "reset descriptor -> one-deep queue drain -> PATCH body {backupPaths: []} without selectionSource -> Phase 1 guard fall-through -> store auto-detection state -> mounts refetch"
    - "CACHEDIR switch -> serialized queue -> PATCH excludeCaches (full map) -> targets.exclude_caches (plan 03-01) -> next-backup union -> argv flag"
    - "container.lastBackup (existing wire field) -> FoldersEditor prop -> narrowing-note gate"
  prohibitions:
    - statement: "The reset must never be sent outside the serialized queue (no fire-and-forget PATCH)"
      status: resolved
      verification: "dom test drives the reset and asserts a single serialized fetch; no overlapping fetch in the timeline"
    - statement: "The CACHEDIR toggle must never ship without the scope tooltip disclosing item-wide effect"
      status: resolved
      verification: "dom test asserts the InfoBubble content renders beside every enabled switch"
    - statement: "Two container PATCHes from one editor must never overlap (T-02-08 class)"
      status: resolved
      verification: "dom test timeline pin: one fetch per drain across mixed backupPaths and excludeCaches mutations"
    - statement: "No fanout affordance into the ExcludesEditor and no snapshot-content comparison (both deferred by ROADMAP note and SELECT-06)"
      status: resolved
      verification: "the exclusions list keeps no interactive elements; the narrowing trigger reads only include counts and lastBackup"
---

<objective>
**As a** BombVault user, **I want to** control exactly what a selection backs up, with an explicit exit when I deselect everything and an honest warning when my selection narrows, **so that** snapshots never silently change scope.

This plan completes the phase interactions over plans 03-01 and 03-02: INTEG-04 UI semantics (per-attempt queue source, the Reset selection exit to auto-detection with confirm and refetch-on-ok, D-05; enriched guard and hint copy), the SELECT-03 narrowing note (event-driven on successful narrowing saves gated by lastBackup, D-02), and the RESTIC-01 toggle UI (per-root switch over the plan 03-01 wire fields with the item-wide scope tooltip and the single serialized PATCH stream, D-06), closing with i18n propagation across 42 locales and the phase gate.

Purpose: Deselecting everything must produce a documented outcome, never a silent flip to auto-detection, and the CACHEDIR effect must never be silent.
Output: Completed INTEG-04/SELECT-03/RESTIC-01 UI, 5 new keys plus 2 text changes across 42 locales, rebuilt web/dist, green full suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-selection-trust-controls/03-CONTEXT.md
@.planning/phases/03-selection-trust-controls/03-RESEARCH.md
@.planning/phases/03-selection-trust-controls/03-PATTERNS.md
@.planning/phases/03-selection-trust-controls/03-UI-SPEC.md
@.planning/phases/03-selection-trust-controls/03-01-SUMMARY.md
@.planning/phases/03-selection-trust-controls/03-02-SUMMARY.md
</context>

<assumption_delta_decision>
Detector signal: the word "fallback" in the INTEG-04 requirement text names the empty-list auto-detection behavior.
Noun under question: the flat backupPaths selection and its empty-list auto-detection semantics.
Decision: no-change. The flagged fallback is the pre-existing, Phase 1 locked semantics of an empty backupPaths list at the persistence boundary; this phase introduces no second selection mechanism, no second source of truth, and no optional variant. It documents the one existing exit (D-05 reset) and pins it: the reset dom test asserts the PATCH body carries no selectionSource, and the Phase 1 guard tests (unchanged) keep tree-source empty saves refused. Together these encode the invariant that the reset is the only sanctioned path back to auto-detection. No promote needed; nothing is added alongside a still-required old form.
</assumption_delta_decision>

## Artifacts this phase-plan produces

- Containers.tsx: SaveDesc per-attempt source field and reset descriptor kind; generalized attemptSave body composition (single fetch per drain carrying all pending PATCH classes); refs lastSavedCount and narrowed-note state; FoldersEditor prop lastBackup: number | null; reset control wiring (useConfirm, refetch-on-ok, shake nonce); excludeCaches map state and onToggleCaches path
- SelectionTree.tsx: CACHEDIR toggle sub-row per root (Toggle with hideLabel, caller-drawn text-xs label, InfoBubble with folders.cachedirScope), new props excludeCaches and onToggleCaches
- api.ts: ContainerMountsResponse.excludeCaches: Record<string, boolean> | undefined and a composed-body PATCH helper used by the queue drain (single fetch carrying backupPaths/selectionSource/excludeCaches as pending)
- i18n keys folders.narrowedNote, folders.resetSelection, folders.resetConfirm, folders.cachedirToggle, folders.cachedirScope plus text changes folders.emptySelectionBlocked and folders.hint, across en+de inline and all 40 locale files
- Dom test cases for reset, narrowing and toggle contracts
- Rebuilt, committed web/dist (phase gate)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-attempt queue source, Reset selection (D-05), narrowing note (D-02), guard and hint copy</name>
  <files>web/src/pages/Containers.tsx, web/src/pages/Containers.tree.dom.test.tsx, web/src/lib/i18n.ts, web/src/lib/locales/*.ts</files>
  <read_first>
  - web/src/pages/Containers.tsx (FoldersEditor: SaveDesc at ~717-722, queue scheduleSave/attemptSave at ~830-936 with the hardcoded tree source at 851, the D-04 zero-include block in onToggle at ~901-925, the mounts load effect at ~793-824, revertFrom, ContainerRow where FoldersEditor is rendered and container is in scope)
  - web/src/lib/api.ts (setBackupPaths at 891-903: selectionSource is included only when truthy; Container.lastBackup at ~27)
  - web/src/pages/Containers.tree.dom.test.tsx (Phase 2 harness)
  - web/src/lib/i18n.ts (folders.emptySelectionBlocked at ~739, folders.hint at ~720, and their de counterparts)
  - .planning/phases/03-selection-trust-controls/03-UI-SPEC.md (Interaction sections 2 and 4, Copywriting Contract rows for the reset, note and both text changes)
  - web/src/lib/useConfirm.tsx (confirm hook signature)
  </read_first>
  <behavior>
  - Dom test: reset flow — clicking the reset control opens the confirm dialog (fail tone, copy naming both consequences); confirming sends exactly one PATCH whose body is {backupPaths: []} with no selectionSource key; on ok the editor refetches mounts and renders the auto-detected selection (no remembered exclusions).
  - Dom test: a tree toggle save still sends selectionSource "tree" (guard stays live for toggles).
  - Dom test: narrowing — a successful save whose attempted include count is lower than the previous successful save, with lastBackup non-null, renders the role=status warn note; no note when lastBackup is null; no note on a net-widening save; a queue-collapsed burst (5 to 3 to 4) nets to narrowing and fires.
  - Dom test: the reset save does not fire the narrowing note (auto-detection is not a narrowed selection; the confirm already documents the consequence).
  - Dom test: the blocked line shown on the zero-include guard references Reset selection; the hint paragraph teaches the reset as the only way back to the automatic default.
  - Dom test: a failed reset PATCH shows the verbatim server error in a fail toast, shakes the button, and leaves selection state untouched.
  </behavior>
  <action>
  Write the failing dom tests first, then implement.
  1. Containers.tsx SaveDesc: add the initiating mutation's selection source (toggles, custom add/remove and structural saves carry the literal "tree"; the reset descriptor carries none). attemptSave at ~851 stops sending the fixed source: when the latest pending descriptor is a reset, the body is exactly {backupPaths: []}; otherwise the live mirror list rides with the descriptor's source (absent source means the api helper omits the key, which the Phase 1 guard passes by design). Stacked cases resolve as RESEARCH Pattern 3 documents: a toggle stacked behind a reset drains the live non-empty list with the tree source; a reset stacked behind a toggle drains [].
  2. Reset control: neutral Button labeled t("folders.resetSelection") at the section foot below the Add row, start-aligned, pt-1. Click opens useConfirm with t("folders.resetConfirm"), default fail tone. On confirm: enqueue the reset descriptor through the SAME queue (never a direct fetch), busy-disable the button, and only on ok set the editor's loaded flag false so the mounts effect refetches (non-optimistic: custom rows are only ever replaced by the refetch); re-initialize lastSavedCount from the refetched mirror. On failure: verbatim toast plus a shake nonce on the button; nothing else changes. Clear pending row busy flags when the attempt settles, following the existing queue bookkeeping.
  3. Narrowing note: add a lastSavedCount ref initialized from the load-time mirror include size (mounts effect) and updated on every successful save with the attempted count; on r.ok, when the attempted count is lower than lastSavedCount AND the lastBackup prop is not null, set the note state. Render one text-xs text-statusWarn line directly under the tree scroll region above the Add row with role="status" (it appears asynchronously after a PATCH resolves). Component state only: cleared on section close/reopen, absent on reopen. Thread lastBackup as a new FoldersEditor prop from ContainerRow (container.lastBackup, wire type number | null).
  4. Copy: apply the two UI-SPEC text changes across all 42 tables in this task — folders.emptySelectionBlocked gains the sentence referencing Reset selection; folders.hint replaces its final sentence (which documents behavior the Phase 1 guard makes impossible) with the reset teaching. Add the three new keys folders.narrowedNote, folders.resetSelection, folders.resetConfirm with the exact en/de copy from the UI-SPEC budget table, propagated to all 40 locale files. No em dashes.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Containers.tree.dom.test.tsx src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts src/lib/i18n.quality.test.ts && npm run build</automated>
  </verify>
  <done>The reset is the documented, serialized, confirmed exit to auto-detection; the guard message teaches it; narrowing saves announce the future-children semantic exactly when they should.</done>
  <acceptance_criteria>
  - grep -c "resetSelection" web/src/pages/Containers.tsx prints at least 1 and grep -c "lastSavedCount" web/src/pages/Containers.tsx prints at least 2
  - grep -rl "narrowedNote" web/src/lib | wc -l prints 41
  - cd web && npx vitest run src/pages/Containers.tree.dom.test.tsx exits 0 including the reset-body, no-source and narrowing cases
  - the dom reset-body assertion verifies the serialized PATCH JSON contains backupPaths and omits the selectionSource key entirely
  - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CACHEDIR.TAG toggle UI over the plan 03-01 fields (D-06) with the single serialized PATCH stream</name>
  <files>web/src/lib/api.ts, web/src/components/SelectionTree.tsx, web/src/pages/Containers.tsx, web/src/pages/Containers.tree.dom.test.tsx, web/src/lib/i18n.ts, web/src/lib/locales/*.ts</files>
  <precondition>Plan 03-01 is landed: go test ./internal/api/ -run 'ExcludeCaches' -v is green at HEAD, so PATCH accepts and mounts serves the excludeCaches field.</precondition>
  <read_first>
  - web/src/lib/api.ts (ContainerMountsResponse at 871-880, setBackupPaths at 891-903, the OkEnvelope helper patterns)
  - web/src/components/SelectionTree.tsx (root rows, the Phase 2 blocked-line presentation-row precedent between a treeitem and its group, props block at ~67-98)
  - web/src/pages/Containers.tsx (task 1 state: SaveDesc kinds, generalized attemptSave, mounts load)
  - web/src/components/Toggle.tsx and web/src/components/InfoBubble.tsx (component contracts: hideLabel, disabled, shakeNonce; bubble chrome)
  - .planning/phases/03-selection-trust-controls/03-UI-SPEC.md (Interaction section 5, Typography hideLabel ruling, Color accent list, UI Considerations E4 rows)
  </read_first>
  <behavior>
  - Dom test: every root row (mounts and standalone custom rows) renders the switch with the caller-drawn label t("folders.cachedirToggle") and the InfoBubble content t("folders.cachedirScope"); unreachable mounts render the switch disabled.
  - Dom test: flipping a switch PATCHes the full excludeCaches map (the mounts-served map with that root flipped); the PATCH never overlaps a queued backupPaths save (one fetch per drain in the timeline, including a caches flip stacked behind an in-flight selection save).
  - Dom test: a failed toggle PATCH reverts to the stored value, shows the verbatim error in a fail toast, and shakes the switch; success is quiet (no toast); in-flight disables the switch.
  </behavior>
  <action>
  Write the failing dom tests first, then implement.
  1. api.ts: add excludeCaches: Record<string, boolean> | undefined to ContainerMountsResponse (doc comment mirroring the Go JSON shape) and give the queue's drain ONE composed-body PATCH helper whose body carries backupPaths, an optional selectionSource, and an optional excludeCaches map, sending only the classes the drain actually owes (a single fetchJSON call per attempt). Migrate attemptSave to it; keep setBackupPaths only if another caller remains (grep first).
  2. Containers.tsx: hold the excludeCaches map in FoldersEditor state initialized from the mounts response (default {}); flipping a root enqueues a caches descriptor through the SAME one-deep queue: the drain carries the live map in the composed body. Optimistic local flip for rendering; on failure revert to the stored value, verbatim toast, shake nonce keyed on the switch; on success quiet. While any PATCH is in flight the switch disables.
  3. SelectionTree.tsx: render one CACHEDIR sub-row per root between the root treeitem and its children group (role presentation wrapper, the blocked-line precedent): flex items-center gap-2, indent 16, py-1 — the Toggle with hideLabel (the sanctioned caller-drawn-label shape; the internal label span would break the tree's 12px register), the caller-drawn label in text-xs text-carbon-textSub, and an InfoBubble with t("folders.cachedirScope") beside it. The row renders regardless of expand state and on custom roots; disabled on unreachable mounts. New props: excludeCaches map and onToggleCaches(hostPath, next).
  4. i18n: add folders.cachedirToggle and folders.cachedirScope with the exact en/de copy from the UI-SPEC budget table and propagate to all 40 locale files in this task.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Containers.tree.dom.test.tsx src/lib/i18n.parity.test.ts src/lib/i18n.orphans.test.ts src/lib/i18n.quality.test.ts && npm run build</automated>
  </verify>
  <done>The per-root switch persists through the single serialized stream, discloses its item-wide scope, and the wire types mirror the Go shape field-for-field.</done>
  <acceptance_criteria>
  - grep -c "excludeCaches" web/src/lib/api.ts prints at least 2 (response field and PATCH helper)
  - grep -c "cachedirToggle" web/src/components/SelectionTree.tsx prints at least 1 and grep -rl "cachedirScope" web/src/lib | wc -l prints 41
  - cd web && npx vitest run src/pages/Containers.tree.dom.test.tsx exits 0 including the no-overlap timeline and disabled-unreachable cases
  - npm run build exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suites, i18n completeness, web/dist committed, Go regression</name>
  <files>web/dist</files>
  <read_first>
  - web/package.json (scripts: test, build, lint)
  - justfile (the Go chain recipes; just is not on PATH on this box, run the underlying commands)
  </read_first>
  <action>
  1. Web: cd web && npm ci && npm test (full vitest suite) && npm run build (tsc --noEmit and vite build) && npm run lint (0 errors; the two pre-existing warn-only items are tracked in STATE.md). Commit the rebuilt web/dist so the embedded SPA is current.
  2. Go regression (the SPA is embedded; prove the binary still builds and the full suite is green): go build ./... && go vet ./... && gofmt -l . && go test ./...
  3. Note in the summary that golangci-lint and just are not on PATH on this Windows box; the CI Lint job is the gate (Phase 2 precedent).
  </action>
  <verify>
    <automated>cd web && npm test && npm run build && npm run lint && cd .. && go build ./... && go vet ./... && gofmt -l . && go test ./...</automated>
  </verify>
  <done>Everything green: full web suite, type check, lint, embedded dist committed, full Go chain.</done>
  <acceptance_criteria>
  - cd web && npm test exits 0; npm run build exits 0; npm run lint exits 0 errors
  - go test ./... exits 0; gofmt -l . prints nothing
  - git status clean after the dist commit (no uncommitted web/dist change)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user actions to PATCH bodies | reset and toggle mutations cross into authenticated writes on the existing endpoint |
| API responses to SPA | mounts-served excludeCaches map and lastBackup feed rendering and gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | concurrent PATCH lost-update from one editor | medium | mitigate | the generalized one-deep queue serializes every container PATCH from FoldersEditor into one fetch per drain; dom test pins no overlapping fetches |
| T-03-08 | Tampering (intent integrity) | silent deselect into auto-detection | high | mitigate | reset is explicit and confirmed with both consequences named; guard message teaches it; the reset body pin keeps the tree guard live for toggles; narrowing is announced (D-02) |
| T-03-09 | Spoofing | CSRF on PATCH fields | medium | accept | existing CSRF middleware on the unchanged endpoint; no new route |
| T-03-10 | Information disclosure | PATCH failure text | low | accept | server errors are already scrubbed (scrubError) before the envelope; the SPA shows them verbatim per house rule |
| T-03-SC | Tampering | package installs | low | accept | zero npm installs this plan (03-RESEARCH.md audit: none) |
</threat_model>

<verification>
- Full web suite green including all new dom contract cases; i18n parity, quality and orphans green across 42 tables
- Full Go chain green (build, vet, gofmt, test) with the embedded SPA rebuilt first
- ROADMAP Phase 3 success criteria 1-4 all observable: preview matches argv truth (03-02), exclusions reviewable (03-02), defined deselect semantics with explicit exit (this plan), CACHEDIR toggle to argv (03-01 plus this plan)
</verification>

<success_criteria>
- INTEG-04 complete: documented UI semantics, explicit reset exit, never a silent auto-detection flip
- SELECT-03 complete: preview plus narrowing note
- RESTIC-01 complete end-to-end: toggle UI through persistence to the argv flag
- Phase gate green; web/dist committed; ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/03-selection-trust-controls/03-03-SUMMARY.md` when done
</output>

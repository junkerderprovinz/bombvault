---
phase: 02-container-panel-tree-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/selectionTree.ts
  - web/src/lib/selectionTree.test.ts
  - web/src/components/SelectionTree.tsx
  - web/src/components/SelectionTree.dom.test.tsx
  - web/src/pages/Containers.tsx
  - web/src/lib/i18n.ts
  - web/src/lib/locales/*.ts
autonomous: true
requirements: [TREE-01, TREE-02, TREE-03, TREE-04, INTEG-01]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - 'In the container panel, unfolding a discovered mount root loads its children lazily: exactly one GET /api/browse call per node on first expand, zero browse calls when the FoldersEditor opens, and re-expanding a collapsed node reuses the editor-lifetime cache with no second call (TREE-01, INTEG-01)'
    - 'Unchecking a volatile subfolder (e.g. a transcoding or cache folder) under a checked mount PATCHes the flat set with the mount still included and the subfolder stored as a !-prefixed exclusion: the rest of the mount stays selected (TREE-02, INTEG-01, success criterion 1)'
    - 'When a toggled include and a stored exclusion are equal host paths or segment-aligned prefixes, the D-01 reducer collapses them deterministically per the Phase 1 selection.go contract: a parent include keeps strictly-below exclusions as dormant entries, and the flat list never holds both a bare and a !-prefixed entry for the same host path (table-tested in selectionTree.test.ts)'
    - 'An empty or single-entry backupPaths list renders without error: zero includes shows every root unchecked (a legacy explicit-none state is displayed, never mutated), one bare include shows its whole subtree checked via prefix arithmetic, and nullish or empty browse listings resolve to defined row states, never a crash'
    - 'The PATCHed backupPaths list is deterministic: identical toggle sequences from identical states produce byte-identical lists (canonical order: sorted bare includes, then sorted !-prefixed exclusions), pinned by a table test in selectionTree.test.ts'
    - 'On reopen, equal or segment-aligned include/exclusion pairs resolve by prefix arithmetic (at/under an E entry excluded; at/under an I entry with an E strictly below mixed; an I strictly below mixed whitelist start-state) without expanding any node (TREE-04)'
    - 'Reconstruction with zero includes renders all roots unchecked and a single bare include renders its subtree checked, both derived from the GET /api/containers/{name}/mounts response alone, with no expansion fetch required (pure list arithmetic)'
    - 'Reconstructed node states are order-insensitive: the same backupPaths set in any permutation yields identical tree states (set semantics; table-tested with shuffled input)'
    - 'Mixed state is the partial state: an indeterminate native checkbox plus aria-checked="mixed" on the treeitem, derived purely from the (I, E) flat lists and therefore correct for collapsed and never-loaded subtrees (TREE-03)'
    - 'Every toggle optimistically updates the (I, E) mirror and immediately PATCHes the full flat list (bare host-path includes plus !-prefixed host-path exclusions) with selectionSource:"tree"; the server is the only selection truth and TREE-04 reconstruction reads the mounts response, never a local selection cache (D-03)'
    - 'Zero mounts render the existing folders.empty line; one or many mounts render identically to today (same row styling and status text, now expandable); no new singular/plural copy is added'
    - 'Tree rows are 12px mono dir="ltr" text with a 16px-per-level paddingInlineStart indent (root rows min-h-8, child rows min-h-7), the whole tree sits in a h-[clamp(12rem,55vh,32rem)] overflow-y-auto scroll region, and every node span truncates with title={path}'
    - 'An expanded empty directory shows one muted folder.none child row, visibly distinct from the per-node loading spinner plus folder.loading row; every per-node browse fetch settles its loading state in a finally, so a failed fetch never leaves an infinite spinner'
  artifacts:
    - path: web/src/lib/selectionTree.ts
      provides: "pure selection-tree logic: EXCLUSION_PREFIX, NodeState, isAtOrUnder, isStrictlyUnder, hostToBrowseRel, browseRelToHost, splitFlatSet, toFlatList, classifyNode, applyToggle (D-01 cycle), loadExpanded, saveExpanded (localStorage bv-tree-expanded-{name}, cap 64)"
    - path: web/src/lib/selectionTree.test.ts
      provides: "node-env table tests: classifier four states + segment alignment, D-01 toggle cycle incl. dormant exclusions and re-include, canonical order/determinism, permutation insensitivity, empty/single-entry, path translation, expansion persistence cap/eviction/throw-safety"
    - path: web/src/components/SelectionTree.tsx
      provides: "the reusable tree component (components/, not pages/, for Phase 4 File Sets): one role=tree whose level-1 treeitems are the mount rows then custom rows (D-02), chevron expand, tri-state checkbox, lazy fetch-on-expand through the editor-lifetime promise cache"
    - path: web/src/components/SelectionTree.dom.test.tsx
      provides: "jsdom tests: expand fires exactly one browse call, cache prevents refetch, toggle PATCH body shape, remount reconstructs states with zero browse calls, empty/loading rows"
    - path: web/src/lib/api.ts
      provides: "wire types caught up to the Phase 1 backend: BrowseResponse gains status and truncated; ContainerMountsResponse gains excluded; setBackupPaths gains the selectionSource option; the envelope gains optional code"
    - path: web/src/pages/Containers.tsx
      provides: "FoldersEditor evolution per D-02: (I, E) mirror derived from the mounts response (selected mounts + custom paths; excluded array), rows rendered through SelectionTree, live-save persist with selectionSource tree"
    - path: web/src/lib/i18n.ts
      provides: "4 new keys folders.treeLabel, folders.truncatedList, folders.retry, folders.emptySelectionBlocked in the en and de inline tables"
    - path: web/src/lib/locales/*.ts
      provides: "the same 4 keys in all 40 locale files (i18n parity test gates every table against the en key set)"
  key_links:
    - from: web/src/pages/Containers.tsx (FoldersEditor load effect)
      to: web/src/lib/api.ts (getContainerMounts)
      via: "derive includes = selected mount sources + custom paths, exclusions = excluded[] (all host form)"
      pattern: "getContainerMounts\\("
    - from: web/src/components/SelectionTree.tsx (expand handler)
      to: web/src/lib/api.ts (browse)
      via: "one call per node on first expand, memoized in the FoldersEditor-lifetime promise cache Map keyed by browse-relative path"
      pattern: "browse\\("
    - from: web/src/pages/Containers.tsx (persist)
      to: web/src/lib/api.ts (setBackupPaths)
      via: "PATCH the full flat list from toFlatList(includes, exclusions) with selectionSource tree"
      pattern: "setBackupPaths\\("
  prohibitions:
    - statement: "The tree must never become an arbitrary filesystem browser: every browsable path descends from a server-discovered mount source or an explicitly added custom path; the tree adds no free-text path entry and never widens the browse surface beyond what FoldersEditor already offers"
      status: unverified
      flagged: true
    - statement: "Expansion comfort state must never leak into the persisted selection: the bv-tree-expanded-* localStorage key carries only expansion, selection reconstruction always comes from the server response, and nothing visual is ever PATCHed into backupPaths"
      status: unverified
      flagged: true
    - statement: "No silent auto-exclusion: every exclusion entry in the stored set must originate from an explicit user toggle; the tree never adds, heuristic-suggests-and-applies, or removes exclusion entries on its own (silent non-backup is the cardinal sin per PROJECT.md)"
      status: unverified
      flagged: true
    - statement: "Unchecking a parent must never delete the exclusions strictly below it: they stay stored dormant so the remembered-partial round-trips (deleting them destroys D-01's remembered partial state)"
      status: unverified
      flagged: true
---

<objective>
Tracer slice plus pure-logic core: the container panel's existing mounts/custom list BECOMES a lazy tri-state tree (D-02) — a mount root unfolds (children fetched on expand only), a subfolder checkbox toggle compiles through the D-01 reducer into the flat backupPaths set and live-saves with selectionSource:"tree" (D-03), and reopening the panel reconstructs exact checked/mixed/excluded states from the mounts response alone (TREE-04). The pure classifier/reducer/translation module lands complete with table tests, and the api.ts wire types catch up to the Phase 1 backend.

Purpose: this is the visible capability of the whole phase (success criteria 1, 2, 3) and the architecture proof that per-node state is pure list arithmetic over includes and exclusions, never derived from loaded children. Everything in plans 02/03 (error rows, guard, keyboard, absorption) expands from this slice.
Output: `web/src/lib/selectionTree.ts` + tests, `web/src/components/SelectionTree.tsx` + dom tests, evolved `FoldersEditor`, extended `web/src/lib/api.ts`, 4 i18n keys x 42 locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-container-panel-tree-selection/02-CONTEXT.md
@.planning/phases/02-container-panel-tree-selection/02-RESEARCH.md
@.planning/phases/02-container-panel-tree-selection/02-PATTERNS.md
@.planning/phases/02-container-panel-tree-selection/02-UI-SPEC.md

Decision key (from 02-CONTEXT.md, cited in task actions): D-01 mixed-click remembered cycle; D-02 the mounts list becomes the tree (no modal, no second presentation); D-03 live-save every toggle with selectionSource:"tree"; D-04 pre-Phase-3 client-side empty guard (lands plan 02); D-05 expansion in localStorage bv-* per container (selection never stored there); D-06 truncated listing notice row (lands plan 02).
</context>

## Artifacts this phase produces (this plan's share)

- `web/src/lib/selectionTree.ts` — NEW named exports: `EXCLUSION_PREFIX` (const, value "!"), `type NodeState = "checked" | "mixed" | "excluded" | "unchecked"`, `isAtOrUnder(path, ancestor)`, `isStrictlyUnder(path, ancestor)`, `hostToBrowseRel(host, hostSourceRoot)`, `browseRelToHost(rel, hostSourceRoot)`, `splitFlatSet(entries)`, `toFlatList(includes, exclusions)`, `classifyNode(hostPath, includes, exclusions)`, `applyToggle(hostPath, includes, exclusions)`, `loadExpanded(containerName)`, `saveExpanded(containerName, paths)`
- `web/src/components/SelectionTree.tsx` — NEW component `SelectionTree` (props include mounts, customPaths, includes, exclusions, hostSourceRoot, containerName, browseCache, onToggle)
- `web/src/lib/api.ts` — edited fields: `BrowseResponse.status`, `BrowseResponse.truncated`, `ContainerMountsResponse.excluded`, `setBackupPaths(name, backupPaths, opts?: { selectionSource?: string })`, envelope `code?: string`
- `web/src/pages/Containers.tsx` — edited: FoldersEditor internals (state shape, persist, row rendering); no exported-signature changes
- `web/src/lib/i18n.ts` + all 40 `web/src/lib/locales/*.ts` — 4 new keys: `folders.treeLabel`, `folders.truncatedList`, `folders.retry`, `folders.emptySelectionBlocked`
- (plans 02/03 add: no new files; they extend SelectionTree.tsx/Containers.tsx and add `SelectionTree.keyboard.dom.test.tsx`, `Containers.tree.dom.test.tsx`, `partitionCustomPaths` in selectionTree.ts)

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — mount row unfolds, subfolder toggle live-saves the flat set, reopen reconstructs exactly</name>
  <reversibility rating="reversible">Pure presentation over an unchanged persistence contract (SELECT-02): git revert restores the previous flat-checkbox FoldersEditor; no stored-data, wire, or schema change.</reversibility>
  <precondition>web/node_modules exists — it is currently absent on this machine; run `cd web && npm ci` first (Node 24, npm 11 verified available).</precondition>
  <files>web/src/lib/api.ts, web/src/lib/selectionTree.ts, web/src/lib/selectionTree.test.ts, web/src/components/SelectionTree.tsx, web/src/components/SelectionTree.dom.test.tsx, web/src/pages/Containers.tsx, web/src/lib/i18n.ts, web/src/lib/locales/*.ts</files>
  <read_first>
    - internal/api/selection.go (whole file — the semantic source of truth: ExclusionPrefix, SplitExclusion, PruneMaximal, NormalizeSelection, the isStrictDescendant segment-aligned prefix rule the TS module must mirror)
    - internal/api/handlers.go lines 1112-1160 (PATCH body incl. SelectionSource + the coded empty-selection refusal), 4191-4200 (classifyReadDirError status kinds), 4328-4335 (browse success response fields)
    - web/src/lib/api.ts lines 1-30 (OkEnvelope), 460-510 (BrowseDirEntry, BrowseResponse, browse fn), 840-880 (MountInfo, CustomPath, ContainerMountsResponse, getContainerMounts, setBackupPaths)
    - web/src/pages/Containers.tsx lines 696-930 (FoldersEditor whole: load-guard effect 721-744, persistPaths 751-769, toggle 771-789, null return 818, mount row markup 836-859, shake-key technique 843-844, custom rows and Add flow below)
    - web/src/components/FolderBrowser.tsx lines 80-120 (doFetch promise chain with ok:false as a distinct state), 220-235 (spinner markup), 275-300 (scroll region + empty row)
    - web/src/components/SnapshotFileTree.tsx lines 100-150 (chevron SVG to copy, indent style to adapt, the untranslated aria-label anti-precedent NOT to copy)
    - web/src/lib/displayPrefs.ts (whole — the house bv-* localStorage discipline: try/catch both directions, narrative why-comment)
    - web/src/pages/Containers.excludesAssistant.dom.test.tsx lines 1-80 (vi.mock api harness + import-after-mock + provider wrapping)
    - web/src/lib/i18n.ts lines 719-731 (en folders.* block) and 2435 area (de block); web/src/lib/locales/fr.ts lines 1-6 (flat key-value locale file shape)
    - 02-UI-SPEC.md sections Spacing, Color (node-state text-tone table), Interaction Contract (structure + selection state machine) — in context above
  </read_first>
  <behavior>
    - Expanding a mount root fires exactly one browse call with the browse-relative path (hostSourceRoot prefix swapped); collapsing and re-expanding hits the promise cache — no second call; opening the FoldersEditor fires zero browse calls
    - Toggling a subfolder under a checked mount root calls setBackupPaths with the flat list containing the mount source bare and "!"+child host path, and selectionSource "tree"
    - Remounting with the same mounts response renders the mount root aria-checked="mixed" and the excluded child in the muted tone, with zero browse calls (pure reconstruction)
    - classifyNode returns excluded for a node at/under an E entry, checked for at/under an I entry with no E strictly below and no I strictly below, mixed when an include applies with an E strictly below OR an I lies strictly below (whitelist), unchecked otherwise; "/c/plex" never matches "/c/plex2/x" (segment-aligned)
    - applyToggle D-01 cycle: unchecked node gains an include (dormant E below preserved and immediately applying); node with its own include loses it (E below stays); checked-via-ancestor-coverage node gains a "!"+node exclusion; mixed node loses its own and strictly-below includes (E stays); excluded node loses the covering E entry and gains its own include when nothing covers it
    - toFlatList emits sorted bare includes then sorted prefixed exclusions — identical sets produce byte-identical arrays
    - hostToBrowseRel/browseRelToHost round-trip; an already-absolute-elsewhere path passes through untranslated (existing Containers.tsx:802 precedent)
    - browse ok:true with zero dirs renders one muted folder.none child row; ok:false renders the scrubbed server text (fallback folder.couldNotRead) inline; every fetch path settles loading in finally
    - All 4 new i18n keys exist in en, de, and every locale file — i18n.parity.test.ts green
  </behavior>
  <action>
    Step 0 — environment: run `cd web && npm ci` (node_modules absent; blocks every check below).

    web/src/lib/api.ts (Wave 0 gap, RESEARCH Pitfall 2): extend `BrowseResponse` with `status?: "ok" | "restricted" | "missing" | "error"` and `truncated?: boolean` (doc comments citing handlers.go classifyReadDirError and maxBrowseEntries=500); extend `ContainerMountsResponse` with `excluded?: string[]` (stored "!" branches in host form, served since Phase 1); change `setBackupPaths` to `setBackupPaths(name: string, backupPaths: string[], opts?: { selectionSource?: string })` sending `{ backupPaths, ...(opts?.selectionSource ? { selectionSource: opts.selectionSource } : {}) }`; add `code?: string` to `OkEnvelope` (coded refusals like empty-selection ride the same envelope). Keep the per-field doc-comment rule (TS mirrors Go JSON camelCase field-for-field).

    web/src/lib/selectionTree.ts — pure module, no React imports, node-env testable. Narrative header comment stating the load-bearing rule: per-node state is list arithmetic over includes I and exclusions E in HOST path space, mirroring internal/api/selection.go semantics (segment-aligned prefix tests; classes pruned per-class; orphan exclusions preserved dormant). Exports exactly as listed in Artifacts above. classifyNode order per 02-RESEARCH Pattern 1 sketch: under-exclusion test first, then include-applies/exclusion-below/include-below for mixed, then checked, else unchecked. applyToggle dispatches on classifyNode plus list membership per D-01 (behavior block above is the pin; the whitelist-flavor mixed click removes the strictly-below includes — the E list below IS the remembered partial, RESEARCH assumption A2's pinned reading — never invent a second UI-side memory). loadExpanded/saveExpanded per D-05: key `bv-tree-expanded-${containerName}`, JSON array of expanded host paths, cap 64 evicting the oldest-expanded (write order = recency), try/catch both directions per displayPrefs.ts (private-window storage throws); a header comment stating selection NEVER goes here.

    web/src/components/SelectionTree.tsx — named export, narrative header, props per Artifacts. ONE role="tree" with aria-label from t("folders.treeLabel"); level-1 role="treeitem" rows are the mount rows (source/dest mono span, statusOk/statusFail lines preserved from Containers.tsx:836-859) followed by custom-path rows (D-02: the list IS the tree — no modal, no second presentation). Expanded children wrap in role="group". Chevron: copy the SnapshotFileTree 10x10 triangle (w-4 box, transition-transform, rotate-90 open, rtl:rotate-180 closed) but route any label through t() — its hard-coded untranslated label is the recorded anti-precedent. Row: click chevron or row body toggles expansion; click checkbox stopPropagation and calls onToggle(hostPath). Native input type=checkbox with accentColor var(--accent); the mixed state via the DOM indeterminate property set in a callback ref (React has no prop for it — first tri-state checkbox in the app, RESEARCH Pattern 5); treeitem carries aria-checked true/mixed/false, never duplicated on the input and never combined with aria-selected. Text tones per UI-SPEC table: checked and mixed text-carbon-text, unchecked text-carbon-textSub, excluded text-carbon-textMuted — no status color on any control. Indent paddingInlineStart depth*16 (logical property); root rows min-h-8, child rows min-h-7; names font-mono dir="ltr" truncate with title={path}; whole tree inside h-[clamp(12rem,55vh,32rem)] overflow-y-auto. Lazy fetch: first expand calls browse(hostToBrowseRel(...)) through the browseCache Map prop (FoldersEditor-lifetime, plain Map path-to-promise, rejections removed from the cache so a retry refetches); loading row = FolderBrowser spinner span + t("folder.loading"); ok:true zero dirs = one muted t("folder.none") row; ok:false = inline muted res.error ?? t("folder.couldNotRead") text (the full no-access/retry treatment is plan 02); every fetch settles the per-node state in finally. Never send the hidden visibility opt-in to browse (BROWSE-04 consistency with FolderBrowser, which does not opt in). Basic roving tabindex: the focused/last-focused treeitem carries tabIndex 0, all others -1 (full APG key map is plan 03).

    web/src/pages/Containers.tsx — evolve FoldersEditor (D-02). State: replace the checked/custom pair with includes/exclusions Sets in HOST space derived in the existing load-guard effect (lines 721-744): includes = selected mount sources union custom paths; exclusions = response excluded array — hold them ABOVE the `if (!open) return null;` (line 818) so they survive section close (Pitfall 3), plus a listings cache `useRef(new Map())` and the hostSourceRoot. persistPaths becomes a flat-list persist calling setBackupPaths(name, entries, { selectionSource: "tree" }) (D-03). onToggle(hostPath) optimistically applies applyToggle to the mirror, re-renders, persists toFlatList(includes, exclusions); on failure revert and replay glim-shake with the row key including the shake nonce (technique at 843-844). The existing mount-row markup and custom rows render INSIDE SelectionTree now (pass mounts, customPaths, sets, cache, onToggle); the Add-custom-path input and Add button stay outside the tree (a control, not a selection row); folders.hint and the quiet folders.saved toast stay. The one-deep serialized queue and the D-04 zero-include block land in plan 02 (interim window noted in output below).

    i18n: add exactly 4 keys to the en block (after folders.empty, ~line 731) and the de block (~2435), copy verbatim from 02-UI-SPEC Copywriting: en folders.treeLabel "Backup folder selection", folders.truncatedList "First 500 entries shown", folders.retry "Try again", folders.emptySelectionBlocked "At least one folder must stay selected. To back up none of this container, turn off Include in schedule."; de drafts pinned in the UI-SPEC i18n Budget. Propagate all 4 to every one of the 40 web/src/lib/locales/*.ts files (translated; parity test fails on any missing key — precedent commit fb6400ce). No em dashes in any user-facing string. Only these 4 keys — everything else reuses existing keys (folder.none, folder.loading, folder.couldNotRead, folders.empty, folders.saved).

    Tests: selectionTree.test.ts — node env, NO jsdom pragma, table-driven literal want slices, covering every behavior case above. SelectionTree.dom.test.tsx — jsdom pragma first line, vi.mock("../lib/api") harness per Containers.excludesAssistant.dom.test.tsx (getContainerMounts/browse/setBackupPaths mocks; component imported AFTER vi.mock; wrapped in the same providers), covering: one-browse-per-expand, cache reuse, toggle PATCH body (bare + prefixed host paths + selectionSource), remount reconstruction with zero browse calls, empty vs loading rows, remount with an exclusions-only stored list shows every root unchecked and is never mutated.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/lib/selectionTree.test.ts src/components/SelectionTree.dom.test.tsx src/lib/i18n.parity.test.ts</automated>
  </verify>
  <done>A mount root in the container panel unfolds with one lazy browse call, a subfolder uncheck PATCHes the flat set (mount bare + "!"+subfolder, selectionSource "tree") leaving the rest of the mount selected, and a remount reconstructs checked/mixed/excluded states from the mounts response with zero browse calls; classifier/reducer tables green; parity green across all 42 locales</done>
  <acceptance_criteria>
    - web/src/lib/selectionTree.ts exports EXCLUSION_PREFIX, NodeState, isAtOrUnder, isStrictlyUnder, hostToBrowseRel, browseRelToHost, splitFlatSet, toFlatList, classifyNode, applyToggle, loadExpanded, saveExpanded and imports nothing from React
    - selectionTree.test.ts exits 0 with table cases for: all four classifyNode states, segment alignment (/c/plex vs /c/plex2), the five applyToggle D-01 transitions, dormant-exclusion preservation, canonical order byte-identity, translation round-trip, empty and single-entry lists
    - api.ts: BrowseResponse contains status and truncated fields; ContainerMountsResponse contains excluded; setBackupPaths accepts a third options argument carrying selectionSource; OkEnvelope contains code
    - SelectionTree.dom.test.tsx exits 0 asserting: exactly one browse call on first expand and none on re-expand, the PATCH body contains the bare mount source and the "!"+child host path with selectionSource "tree", and remount reconstruction issues zero browse calls
    - FoldersEditor derives includes from selected mounts plus custom and exclusions from the response excluded array; state lives above the null return (survives section close)
    - i18n.ts and all 40 locale files contain the 4 new keys; npx vitest run src/lib/i18n.parity.test.ts exits 0
    - SelectionTree.tsx contains role="tree" with aria-label from folders.treeLabel and an indeterminate callback ref; no raw hex and no status-color class on the checkbox or chevron
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure-module completeness — determinism, permutation insensitivity, persistence bounds</name>
  <files>web/src/lib/selectionTree.test.ts</files>
  <read_first>
    - web/src/lib/selectionTree.ts (whole — the module under test as landed by Task 1)
    - web/src/lib/selectionTree.test.ts (whole — extend, do not rewrite passing cases)
    - internal/api/selection.go lines 44-110 (the Go pruning/normalization invariants the TS mirror must not drift from)
  </read_first>
  <behavior>
    - Identical toggle sequences from identical starting states produce byte-identical flat lists (run each sequence twice, deep-equal)
    - The same backupPaths set in any permutation (shuffle the flat list across several seeds) yields identical classifyNode results for a fixed node corpus — set semantics, order-insensitive reconstruction
    - An exclusions-only flat list splits to zero includes and non-empty exclusions (explicit-none carrier displayed, never repaired or mutated client-side)
    - loadExpanded on a missing/throwing localStorage returns an empty array; saveExpanded beyond 64 entries evicts the oldest-expanded first and preserves write order as recency; both directions survive a throwing localStorage stub (no exception escapes)
    - toFlatList of an includes-empty set is empty (never emits exclusions-only prefixes when includes exist elsewhere — the function is total over its inputs, no hidden item-level knowledge)
  </behavior>
  <action>
    Extend web/src/lib/selectionTree.test.ts with the pinned edge tables (behavior block above). Determinism table: apply the same three-toggle sequence from the same starting (I, E) twice and assert deep equality of toFlatList outputs. Permutation table: take a mixed flat set (two includes, two exclusions, one nested), generate permutations, splitFlatSet each, assert identical classifyNode outcomes across a fixed corpus of at/under/strictly-below nodes. Persistence table: stub the global localStorage (jsdom-free: assign a mock onto globalThis for the module's guarded accessors) with a throwing implementation and a capped-array simulation — assert loadExpanded returns [] under throw, saveExpanded never throws, and the 64-cap evicts in write order. Add a why-comment citing D-05 (expansion is comfort state only) and the explicit-none carrier note citing Phase 1's storedDataIsGone decision. Do not modify selectionTree.ts unless a table exposes a real defect — if it does, fix the module and re-run the full file.
  </action>
  <verify>
    <automated>cd web && npm test</automated>
  </verify>
  <done>Full web vitest suite green including the new determinism, permutation, explicit-none, and persistence-bound tables; no regression in any pre-existing test</done>
  <acceptance_criteria>
    - selectionTree.test.ts contains table cases named/covering: toggle-sequence determinism (deep-equal repeat), permutation-insensitive classification, exclusions-only split (zero includes), localStorage throw-safety both directions, cap-64 oldest-eviction with write-order recency
    - cd web && npm test exits 0 (whole suite — includes i18n parity/quality and all pre-existing dom tests)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA -> /api/browse, PATCH /api/containers/{name} | Existing authed/CSRF-protected routes; the tree builds PATCH bodies and browse queries from strings the server itself served |
| Server listings -> rendered DOM | Directory names are external data rendered into the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | PATCH body construction (SelectionTree onToggle -> setBackupPaths) | high | mitigate | Every toggled entry derives from server-served paths only (mounts response, browse dirs); no free-text path entry in the tree; host-form translation is prefix arithmetic over the two served roots; the server re-validates and contains (Phase 1 toContainerPath + os.Root) as the inherited backstop |
| T-02-02 | Tampering (user intent) | optimistic mirror vs server truth (FoldersEditor persist) | medium | mitigate | Single selection truth: every toggle round-trips the full flat list (D-03); failure reverts and shakes; nothing derived from localStorage ever reaches backupPaths |
| T-02-03 | Information Disclosure | directory-name rendering (SelectionTree rows) | low | mitigate | React text-node auto-escaping; names render like every existing path (font-mono dir="ltr" spans); no dangerouslySetInnerHTML anywhere; browse error text is already server-scrubbed and shown verbatim |
| T-02-04 | Tampering | localStorage expansion key (loadExpanded/saveExpanded) | low | mitigate | Guarded JSON parse; hard 64-entry cap bounds growth; try/catch both directions; parsed values used only as expansion hints, never as paths to fetch or PATCH |
| T-02-SC | Tampering | package installs | low | accept | Zero npm installs this phase (RESEARCH Package Legitimacy Audit: none; UI kits and state libraries banned by CLAUDE.md) |
</threat_model>

<verification>
- cd web && npx tsc --noEmit — strict typecheck green
- cd web && npm test — full vitest suite green (selectionTree tables, SelectionTree dom slice, i18n parity/quality/orphans across 42 locales, all pre-existing tests unmodified)
- cd web && npm run lint — eslint + the 8 bombvault/* house rules green (no disable comments; declare exceptions in eslint.config.js only if a rule genuinely blocks)
- Manual smoke (optional, pre-verify): open the container panel, unfold an appdata mount, uncheck a subfolder, close and reopen the section — states and expansion persist, PATCH observed in devtools network tab
</verification>

<success_criteria>
1. Success criterion 1 (unfold + uncheck volatile subfolder keeps the rest) demonstrable through the tracer dom test
2. Success criterion 2 (cascade include, mixed parents, remembered partial via dormant exclusions) pinned by classifier/reducer tables and the remount reconstruction test
3. Success criterion 3 (reopen exactness) pinned by the zero-browse-call reconstruction test and permutation tables
4. Wire types no longer lag the Phase 1 backend (RESEARCH Pitfall 2 closed)
</success_criteria>

## Flagged assumptions

- TREE-01 edge-probe classification returned `unclassified` (#1110 — flagged assumption, never auto-resolved). Planned from TREE-01's requirement text + D-02/D-05 + RESEARCH Pattern "lazy loading states" and pinned by the dom tests (fetch-on-expand only, one call per node, cache reuse, zero calls at open). Residual interpretation risk surfaced here rather than silently resolved.
- TREE-03 edge-probe classification returned `unclassified`. Planned from TREE-03 verbatim + D-01 + RESEARCH Pattern 2 (the exclusion list below a node IS the remembered partial state — dormant exclusions round-trip; the whitelist-flavor mixed click removes strictly-below includes per the pinned research reading, assumption A2). Pinned by applyToggle tables and the remount mixed-state test.
- Canon security candidates (path traversal via crafted node paths, XSS via directory names, oversized PATCH bodies) were referred to existing coverage — server-side containment/validation landed in Phase 1 and React escaping — not minted as new prohibitions.

<assumption_delta_decision>
Noun: the single flat backupPaths selection set (unchanged since Phase 1).
Decision: no-change — the detector fired on the phrase "custom path", which is the long-standing CustomPath domain noun (GET /api/containers/{name}/mounts serves custom[] today), not a derived-to-chosen modeling transition. This phase adds zero representations: the tree is a view over the one flat set (SELECT-02); D-02 explicitly forbids a second presentation. Invariant test encoding the intent: the permutation-insensitivity + remount-reconstruction tables in selectionTree.test.ts / SelectionTree.dom.test.tsx (same set, any shape, one rendering).
</assumption_delta_decision>

<output>
Create `.planning/phases/02-container-panel-tree-selection/02-01-SUMMARY.md` when done.

The SUMMARY MUST include an "Interim window" note (checker-accepted, wave-ordered): between this plan and plan 02 Task 2, (a) unchecking the last include of an item is NOT yet blocked client-side (D-04 lands plan 02) — the PATCH would carry an exclusions-only list the server accepts as explicit-none, and (b) rapid toggles use the simple await-per-save path — the serialized one-deep queue lands plan 02. Both are UI-only windows over correct server semantics. Re-surface at the phase gate if plan 02 did not execute.
</output>

---
phase: 01-selection-engine-restore-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/handlers.go
  - internal/api/browse_contract_test.go
autonomous: true
requirements: [BROWSE-01, BROWSE-02, BROWSE-03, BROWSE-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Listing a tree node's children is a single per-request ReadDir: cheap, per-node, no recursion, and no hasChildren emptiness probe (every directory is expandable; an empty directory lists zero children)"
    - "A node listing returns each directory entry exactly once (no duplicates)"
    - "Listing an empty directory returns entries:[] with status:'ok' (empty list + ok is the real-empty signal, never an error)"
    - "Entries are returned in lexical order (the existing comparator), stable across repeated calls"
    - "At the listing cap (maxBrowseEntries = 500): exactly 500 entries returns truncated:false; 501+ returns the lexically-first 500 with truncated:true"
    - "Requesting the browsable root lists the root's children; a path resolving outside the root is rejected with the byte-identical traversal response (no status field)"
    - "Listing a non-existent path returns status:'missing' and a permission-denied path returns status:'restricted' (both in the HTTP 200 envelope with the generic scrubbed message)"
    - "Order of entries listed through symlinks is identical to direct listings (os.Root resolves containment, it does not reorder)"
    - "Entry names are returned verbatim: no truncation, no case folding, no Unicode normalization"
    - "hidden=1 includes dot-prefixed entries at the root and at every depth; without it they are excluded"
    - "Only dot-prefixed names are hidden: mixed directories list both classes, separated only by the flag"
    - "An empty directory returns entries:[] with and without hidden=1 (the flag never fabricates entries)"
    - "Entry order is identical with and without hidden=1 (the flag filters, never reorders)"
    - "Hidden-entry visibility agrees between the tree listing and the existing folder browser: same dot-prefix rule, byte-identical names, and the four existing browse contract tests pass unmodified"
  artifacts:
    - path: internal/api/handlers.go
      provides: "handleBrowse hardened: os.OpenRoot containment, maxBrowseEntries const (500) + truncated bool, per-listing status (ok/restricted/missing/error), ?hidden=1 opt-in; existing response fields byte-identical"
    - path: internal/api/browse_contract_test.go
      provides: "NEW contract tests: status trio, cap+truncated, hidden delta, escaping-symlink fixture (GOOS-guarded), error-classifier table"
  key_links:
    - from: internal/api/handlers.go (handleBrowse)
      to: os.OpenRoot / Root.Open (Go 1.24+ stdlib)
      via: "kernel-enforced containment after the lexical paths.Resolve first reject"
      pattern: "os\\.OpenRoot"
    - from: internal/api/handlers.go (handleBrowse read-error branch)
      to: errors.Is on *fs.PathError
      via: "status classification carries the error KIND, never the path"
      pattern: "fs\\.ErrPermission"
  prohibitions:
    - statement: "The traversal/absolute rejection response gains no new fields and stays byte-identical; contract drift on untouched branches is forbidden"
      status: unverified
      flagged: true
    - statement: "No hasChildren hint or emptiness probe is ever added to the listing; expansion probes are an N+1 latency multiplier on shfs/FUSE"
      status: unverified
      flagged: true
---

<objective>
Browse node-listing contract: `GET /api/browse` becomes the cheap, capped, symlink-safe, status-carrying per-node listing the Phase 2 tree will expand against — extended ADDITIVELY only, with the existing FolderBrowser contract pinned byte-identically.

Purpose: BROWSE-01..04 in one coherent slice — the tree must never become an arbitrary filesystem probe (os.Root containment), must distinguish empty from unreadable (status trio), must not hang on huge dirs (cap 500 + truncated), and must agree with the existing browser on hidden entries (?hidden=1 delta pinned both ways).
Output: hardened `handleBrowse` + `browse_contract_test.go`; zero new endpoints, zero web/ changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-selection-engine-restore-safety/01-CONTEXT.md
@.planning/phases/01-selection-engine-restore-safety/01-RESEARCH.md
@.planning/phases/01-selection-engine-restore-safety/01-PATTERNS.md
@.planning/phases/01-selection-engine-restore-safety/01-VALIDATION.md

Decision key (used in task actions below; numbering follows 01-CONTEXT.md `<decisions>` order):
D-05..D-08 = browse Q1-Q4 (additive extension only; status trio; NO hasChildren; cap ~500 + truncated, lexical sort).
D-01..D-04 = encoding Q1-Q4 (plan 01). D-09..D-12 = INTEG-04 Q1-Q4 (plan 04). D-13..D-16 = restore Q1-Q4 (plan 03).
</context>

## Artifacts this phase produces (this plan's share)

- `internal/api/handlers.go` — `maxBrowseEntries` const (= 500, near `browseDirEntry`); unexported read-error classifier (e.g. `classifyReadDirError(err error) string`); hardened `handleBrowse` (os.Root, status, truncated, hidden param)
- `internal/api/browse_contract_test.go` — NEW test file
- (plans 01/03/04 add: selection helpers, `mapRestorePaths`, `errEmptySelection`, mounts `excluded` field, `RestoreDeps.SkippedPaths`)

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — containment-safe listing with the status trio, wired through the real router</name>
  <files>internal/api/handlers.go, internal/api/browse_contract_test.go</files>
  <read_first>
    - internal/api/handlers.go lines 4140-4210 (handleBrowse: query param, empty-subpath root listing, paths.Resolve reject, os.ReadDir, dirs-only filter, hidden skip, rel-path build, lexical sort, success payload)
    - internal/api/handlers.go lines 2930-2940 (browseDirEntry — entry shape unchanged)
    - internal/api/handlers.go lines 56-131 (writeJSON/okEnvelope/failEnvelope + scrub discipline) and lines 60-80 region for the nolint-with-justification style on the existing browse log line
    - internal/api/handlers_test.go lines 1520-1630 (newBrowseRouter harness + the four existing browse tests that MUST stay green unmodified)
    - 01-PATTERNS.md section "handleBrowse extension" (in context above) and 01-RESEARCH.md R2/R6
  </read_first>
  <behavior>
    - GET /api/browse?path=<existing-dir> returns ok:true with the existing fields (root, path, dirs) PLUS status:"ok"
    - GET /api/browse?path=<non-existent> returns ok:false, error "could not read directory" (verbatim), status:"missing"
    - A read error classified as permission-denied returns the same generic error string with status:"restricted" (classifier unit-tested on all OSes; fixture GOOS-guarded)
    - A path escaping the root via a symlink is rejected by os.Root instead of listed (GOOS-guarded fixture on Windows dev; runs on Linux CI)
    - The traversal and absolute-path rejection responses stay byte-identical to today: no status field anywhere on those branches
  </behavior>
  <action>
    Harden handleBrowse in place per D-05/D-06 (CONTEXT browse Q1/Q2) — one endpoint, additive only. Keep paths.Resolve as the cheap FIRST reject so the traversal/absolute response stays byte-identical (no status field on those branches). Replace os.ReadDir(abs) with per-request containment: os.OpenRoot(h.cfg.HostMountRoot) + defer root.Close(), then root.Open(rel) where rel is the Resolve-validated subpath (empty subpath opens "." per the stdlib Root contract), then f.ReadDir(-1). Keep the existing explicit lexical sort and the dirs-only filter. Classify the read error through a small unexported helper (e.g. classifyReadDirError) using errors.Is on fs.ErrPermission -> "restricted", fs.ErrNotExist -> "missing", anything else (including an os.Root escape rejection) -> "error"; keep the "could not read directory" string verbatim and ADD "status" to that failure envelope; ADD "status":"ok" to the success payload. The status field carries the error KIND, never the path (scrub discipline). Preserve the existing log line's nolint-with-justification style for any new log line. No hasChildren field, no emptiness probe, no recursion (per D-07). Entry shape browseDirEntry unchanged.

    Tests in NEW internal/api/browse_contract_test.go (existing handlers_test.go browse tests stay UNMODIFIED): TestBrowseStatusOkOnEmpty (empty dir -> entries:[] + status:"ok"), TestBrowseStatusMissing (ENOENT), TestBrowseStatusRestricted via the GOOS-guarded chmod fixture (skip on Windows with a loud message — the house POSIX-skip convention), a plain-Go table test for classifyReadDirError over constructed *fs.PathError values (runs on every OS), and a GOOS-guarded escaping-symlink fixture asserting the symlink escape is rejected, not listed (RESEARCH Pitfall 5). Use the newBrowseRouter harness + doJSON; assert m["ok"], never status codes except the trivial 200.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/ -run 'TestBrowse' -count=1</automated>
  </verify>
  <done>Listing is kernel-contained via os.Root behind the unchanged lexical first reject; every read outcome lands in the HTTP 200 envelope carrying a status kind; the four pre-existing browse contract tests pass unmodified</done>
  <acceptance_criteria>
    - handleBrowse opens os.OpenRoot(h.cfg.HostMountRoot) per request and lists via Root.Open + ReadDir(-1) (source assertion); paths.Resolve still runs first and its rejection response is unchanged
    - go test ./internal/api/ -run 'TestBrowseStatus' -count=1 exits 0 covering ok-on-empty, missing, and the classifier table
    - go test ./internal/api/ -run 'TestBrowseListsMountRoot|TestBrowseListsSubpath|TestBrowseRejectsTraversal|TestBrowseRejectsAbsolutePath' -count=1 exits 0 with those tests unmodified (git diff on their assertions shows no change)
    - The symlink-escape and restricted fixtures carry an explicit runtime.GOOS skip with a loud message (they prove on Linux CI)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Listing cap with truncation flag + hidden-entry opt-in</name>
  <files>internal/api/handlers.go, internal/api/browse_contract_test.go</files>
  <read_first>
    - internal/api/handlers.go — the handleBrowse filtering/sort/payload region as left by Task 1 (filter dirs, hidden skip, lexical sort, success payload)
    - internal/api/handlers.go lines 2930-2940 (browseDirEntry — maxBrowseEntries goes near here)
    - internal/api/browse_contract_test.go — the Task 1 file this extends
    - 01-RESEARCH.md R1 (cap order of operations) and R8 (hidden consistency)
  </read_first>
  <behavior>
    - A directory with 501 entries returns the lexically-first 500 with truncated:true; exactly 500 entries returns truncated:false
    - Default (no flag): dot-prefixed entries are excluded, as today
    - hidden=1: dot-prefixed entries are included at the root and at every depth; all non-hidden entries identical and identically sorted versus the default call
    - An empty directory returns entries:[] with and without hidden=1
    - Sorting happens on the filtered slice, then truncation (lexically-first 500)
  </behavior>
  <action>
    Add const maxBrowseEntries = 500 near browseDirEntry (per D-08 / RESEARCH R1; 500 is the locked order-of-magnitude, this is the exact value). Pipeline order: ReadDir(-1) -> filter (dirs-only, then dot-dir skip UNLESS the hidden opt-in) -> existing lexical sort on the filtered slice -> truncate to maxBrowseEntries -> truncated = the kept count exceeded the cap before truncation. Read the flag as r.URL.Query().Get("hidden") == "1" (per D-05 the flag is opt-in; absence keeps today's byte-identical default behavior). ADD "truncated":bool to the success payload alongside status. Everything else about the response shape stays as Task 1 left it.

    Tests appended to internal/api/browse_contract_test.go: TestBrowseHidden — one fixture dir mixing dot-prefixed and normal entries (plus a name that merely contains a dot mid-name, which must never be hidden): default call excludes dot-entries, hidden=1 call includes them, otherwise the two responses are identical and identically sorted; empty-dir case with and without the flag. TestBrowseCap — fixture with 501 entries asserting len(dirs)==500, lexically-first 500, truncated:true; a 500-entry fixture asserting truncated:false (build fixtures with deterministic names; keep the fixture creation cheap). Existing tests (Task 1's and handlers_test.go's) stay green unmodified.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestBrowse' -count=1 && go vet ./internal/api/ && test -z "$(gofmt -l internal/api)"</automated>
  </verify>
  <done>Large directories return a deterministic capped listing with truncated:true; hidden entries are a pinned opt-in delta with the default byte-identical; the full TestBrowse family is green</done>
  <acceptance_criteria>
    - maxBrowseEntries is defined as 500 in internal/api/handlers.go and the truncation happens AFTER the lexical sort of the filtered slice (source assertion)
    - go test ./internal/api/ -run 'TestBrowseCap' -count=1 exits 0 asserting both the 501 and 500 cases with the truncated flag values above
    - go test ./internal/api/ -run 'TestBrowseHidden' -count=1 exits 0 asserting the delta both ways, the mid-dot name never hidden, and empty-dir behavior with and without the flag
    - go test ./internal/api/ -run 'TestBrowse' -count=1 exits 0 (whole browse family, existing tests unmodified)
    - go vet ./internal/api/ clean; gofmt reports nothing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query string -> filesystem listing | ?path= is untrusted input that must never escape the host mount root, directly or via symlink |
| Listing response -> SPA | Entry names and error text cross to the client; must not leak host layout or raw paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | handleBrowse direct-address symlink | high | mitigate | os.Root kernel-enforced containment (symlinks may not reference outside the root) layered behind the unchanged paths.Resolve lexical reject; GOOS-guarded escaping-symlink fixture proves it on CI |
| T-01-06 | Information Disclosure | handleBrowse traversal | high | mitigate | Browsable universe stays under HostMountRoot only; byte-identical traversal/absolute rejection; no new route (behind the same authGate/csrfGate chain) |
| T-01-07 | Denial of Service | handleBrowse huge directories | medium | mitigate | 500-entry cap + truncated flag; single ReadDir per request; no recursion, no emptiness probes |
| T-01-08 | Information Disclosure | browse error text / status field | medium | mitigate | Generic "could not read directory" string kept verbatim; status carries the error KIND, never the path; existing scrub discipline on envelopes |
| T-01-SC | Tampering | package installs | low | accept | No package installs — os.Root is Go 1.24+ stdlib (RESEARCH verified via Context7) |
</threat_model>

<verification>
- `go test ./internal/api/ -run 'TestBrowse' -count=1` green (quick sampling path from 01-VALIDATION.md)
- The four pre-existing browse contract tests pass UNMODIFIED (contract drift check)
- `go build ./... && go vet ./... && gofmt -l .` clean
- No web/ changes: `git status --porcelain web/` empty (additive JSON fields are invisible to the existing typed client; api.ts types land in Phase 2 per RESEARCH)
</verification>

<success_criteria>
1. Listing a node's children is a cheap per-node call distinguishing empty from unreadable with scrubbed messages (criterion 4, halves 1-2)
2. Listing never escapes its root boundary, even through symlinks (criterion 4, half 3)
3. Hidden-entry visibility is a pinned, additive opt-in agreeing with the existing folder browser (criterion 4, half 4)
</success_criteria>

## Flagged assumptions
- BROWSE-02 edge-probe classification returned `unclassified` (#1110 — flagged assumption, never auto-resolved). The status-trio semantics are planned from CONTEXT browse Q2 (ok/restricted/missing, scrubbed messages, HTTP 200 envelope) and covered by TestBrowseStatus + the classifier table. Residual interpretation risk is surfaced here rather than silently resolved.

<assumption_delta_decision>
Noun: the single browsable universe under HostMountRoot (GET /api/browse).
Decision: no-change — the detector's only signal was the word "chosen" in restore prose (plan 03 territory), not a browse-model transition. The listing surface stays ONE endpoint extended additively (per D-05); no second browsable universe is added alongside.
</assumption_delta_decision>

<output>
Create `.planning/phases/01-selection-engine-restore-safety/01-02-SUMMARY.md` when done
</output>

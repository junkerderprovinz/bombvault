---
phase: 02-container-panel-tree-selection
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - web/src/components/SelectionTree.tsx
  - web/src/components/SelectionTree.keyboard.dom.test.tsx
  - web/src/lib/selectionTree.ts
  - web/src/lib/selectionTree.test.ts
  - web/src/pages/Containers.tsx
  - web/src/pages/Containers.tree.dom.test.tsx
  - web/dist
autonomous: true
requirements: [TREE-05, TREE-01, TREE-04, INTEG-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - 'The tree is fully operable by keyboard: Right expands a closed node with focus staying on the parent and moves to the first child only on a second Right (a no-op while children are still loading), Left collapses an open node / focuses the parent from a closed child / does nothing on a closed root, Down and Up move focus between treeitems without expanding, Home and End jump to the first and last focusable treeitem, Enter toggles expansion, and Space toggles only the focused node''s checkbox — pinned in SelectionTree.keyboard.dom.test.tsx via document.activeElement assertions'
    - 'Keyboard operations are idempotent: Right on an expanded or still-loading node is a no-op (focus stays), Left on a collapsed root is a no-op, and Space toggled twice returns the exact prior (I, E) flat list'
    - 'aria-expanded is rendered on every treeitem (an expanded empty directory honestly reports true with zero children), aria-checked is true/mixed/false on the treeitem and never combined with aria-selected, aria-level, aria-setsize and aria-posinset are computed from the flat model on every node uniformly (not only after lazy load), exactly one treeitem carries tabIndex 0 (roving tabindex), and the tree element carries its accessible name from folders.treeLabel (TREE-05)'
    - 'Notice rows (truncated, no-access, empty, loading) are not treeitems and are excluded from setsize/posinset counts — geometry counts only real nodes'
    - 'Custom entries that lie under a reachable mount source are absorbed into that mount''s tree state (rendering the mount mixed per the whitelist start-state) and filtered from the custom row list by segment-aligned prefix test — one presentation of every path, no duplicates on the same screen (D-02, RESEARCH Q1)'
    - 'Closing and reopening the FoldersEditor section restores expanded nodes from the bv-tree-expanded-{name} localStorage key and reuses the editor-lifetime listings cache without refetch, while selection state always re-derives from the server-round-tripped (I, E) mirror — expansion is comfort, never truth (D-05, TREE-04)'
    - 'The FoldersEditor integration test pins the wire contract end-to-end: a subfolder uncheck PATCHes backupPaths containing !-prefixed host paths with selectionSource:"tree"; the last-include toggle fires no request; mount and custom roots are all expandable lazy roots (INTEG-01)'
  artifacts:
    - path: web/src/components/SelectionTree.keyboard.dom.test.tsx
      provides: "jsdom keyboard/ARIA tests modeled on DropdownListbox.keyboard.dom.test.tsx: full APG key map, roving tabindex, focus placement, aria geometry, idempotency"
    - path: web/src/lib/selectionTree.ts
      provides: "partitionCustomPaths(customPaths, mountSources) — pure segment-aligned split into under-mount (absorbed) and standalone lists"
    - web/src/lib/selectionTree.test.ts
      provides: "tables for partitionCustomPaths (under mount, exact-mount-root match stays a mount row, sibling path stays standalone)"
    - path: web/src/pages/Containers.tree.dom.test.tsx
      provides: "FoldersEditor integration tests through the vi.mock api harness: wire contract, D-04 no-call, reopen restoration, absorption rendering"
    - path: web/dist
      provides: "rebuilt embedded SPA (tsc --noEmit && vite build output) committed — the binary embeds web/dist"
  key_links:
    - from: web/src/components/SelectionTree.tsx (key handler)
      to: web/src/lib/selectionTree.ts (classifyNode + applyToggle via onToggle)
      via: "Space routes through the same toggle path as checkbox clicks — one selection semantics for pointer and keyboard"
      pattern: "onToggle\\("
    - from: web/src/pages/Containers.tsx (FoldersEditor render)
      to: web/src/lib/selectionTree.ts (partitionCustomPaths)
      via: "custom rows filter to standalone entries; under-mount includes join the tree state under their mount root"
      pattern: "partitionCustomPaths\\("
    - from: web/src (SPA source)
      to: web/dist (embedded build output)
      via: "npm run build after every web change; dist committed or go build embeds a stale SPA"
      pattern: "vite build"
  prohibitions:
    - statement: "No second presentation of the same selection may appear on one screen: no modal, no fine-selection button, no parallel flat list beside the tree — the mounts/custom list IS the tree (D-02)"
      status: unverified
      flagged: true
---

<objective>
Keyboard and ARIA completeness plus integration closure: the full APG TreeView key map with roving tabindex and lazy-node geometry (TREE-05), the custom-sub-include absorption that keeps one presentation per path (D-02/INTEG-01), reopen behavior that restores expansion comfort while selection truth stays server-derived (D-05/TREE-04), the FoldersEditor integration test, and the phase gate — full suite, lint, build, committed web/dist, Go regression.

Purpose: delivers success criterion 4 (full keyboard + ARIA tree/checkbox roles) and closes the phase: the embedded SPA in the binary must reflect the new panel before verification.
Output: keyboard test file, partitionCustomPaths + tables, integration test, SelectionTree/Containers completion, rebuilt committed web/dist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-container-panel-tree-selection/02-CONTEXT.md
@.planning/phases/02-container-panel-tree-selection/02-RESEARCH.md
@.planning/phases/02-container-panel-tree-selection/02-PATTERNS.md
@.planning/phases/02-container-panel-tree-selection/02-UI-SPEC.md
@.planning/phases/02-container-panel-tree-selection/02-01-SUMMARY.md
@.planning/phases/02-container-panel-tree-selection/02-02-SUMMARY.md

Decision key: D-02 one tree, rows become roots; D-05 expansion persisted in localStorage bv-* per container (capped), selection always server-reconstructed. RESEARCH Pattern 3 (one tree, roots are the existing rows), Pattern 4 (APG TreeView keyboard map + the easy-to-get-wrong list), Pitfall 7 (aria-expanded on leaves / setsize on lazy nodes), Open Questions Q1 (absorption recommendation, followed) and Q2 (component persistence kept — no forced refetch on reopen).
</context>

## Artifacts this phase produces (this plan's share)

- `web/src/components/SelectionTree.keyboard.dom.test.tsx` — NEW test file (jsdom pragma; no production exports)
- `web/src/lib/selectionTree.ts` — NEW export `partitionCustomPaths(customPaths: readonly string[], mountSources: readonly string[]): { underMount: string[]; standalone: string[] }`
- `web/src/pages/Containers.tree.dom.test.tsx` — NEW integration test file
- `web/src/components/SelectionTree.tsx` / `web/src/pages/Containers.tsx` — edited: keyboard handler, aria geometry, absorption wiring, expansion restore (no exported-surface changes)
- `web/dist/**` — rebuilt and committed

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TREE-05 — APG keyboard map, roving tabindex, lazy-node aria geometry</name>
  <files>web/src/components/SelectionTree.tsx, web/src/components/SelectionTree.keyboard.dom.test.tsx</files>
  <read_first>
    - web/src/components/SelectionTree.tsx (whole — as landed by plans 01/02: row structure, render states, basic roving tabindex from plan 01)
    - web/src/components/DropdownListbox.keyboard.dom.test.tsx (whole, 126 lines — the house keyboard-test harness: beforeEach scrollIntoView stub, document.activeElement assertions, await act + fireEvent.keyDown pattern)
    - 02-RESEARCH.md Pattern 4 and the APG keyboard reference table; 02-UI-SPEC.md Interaction Contract sections Structure and Keyboard map — in context above
    - web/src/lib/selectionTree.ts (classifyNode — Space routes through the same onToggle path as clicks)
  </read_first>
  <behavior>
    - Right on a closed root: expands, document.activeElement stays on the root, no child fetch assert needed beyond the existing lazy path; Right again once children are loaded: focus moves to the first child; Right while children are loading (deferred browse promise): no-op, focus stays
    - Left on an open node: collapses (focus stays); Left on a closed child: focus moves to its parent treeitem; Left on a closed level-1 root: no-op
    - Down/Up: focus moves to the next/previous focusable treeitem in visual order, never expanding or collapsing anything
    - Home/End: focus jumps to the first/last focusable treeitem across all roots (one tree, mounts then customs)
    - Enter on a parent: toggles expansion; Enter on a leaf: no extra action
    - Space: toggles the focused node's checkbox only — the PATCH body changes for exactly that node; Space twice returns the exact prior flat list (idempotency)
    - Exactly one treeitem in the DOM carries tabIndex 0 at any time; it is the focused (or last-focused) node
    - Every treeitem carries aria-expanded, aria-checked, aria-level, aria-setsize, aria-posinset; notice rows (folder.none, truncated, no-access, loading) carry none of the treeitem geometry
  </behavior>
  <action>
    Implement the full APG TreeView checkbox-variant key map in SelectionTree.tsx per the UI-SPEC keyboard table (behavior block is the pin). Key handling lives in one onKeyDown on the tree element; maintain the focus model as a host-path (or node id) in component state, rendering tabIndex 0 on that node and -1 elsewhere; after collapse/expand moves, keep focus on the node the APG pattern says keeps it. The expanded-but-loading state from plan 01's fetch path must make a second Right a no-op (the loading row is not focusable). aria-level counts from the flat rendered model (mount/custom roots level 1); aria-setsize/aria-posinset count ONLY real treeitem siblings within each group — notice rows are plain rows outside the count (RESEARCH Pitfall 7); compute geometry uniformly for every node whether or not its children have loaded. aria-checked semantics stay on the treeitem (plan 01 rule); do not add aria-selected anywhere. The accessible name of the tree element stays folders.treeLabel. Enter toggles expansion on any node; Space is the only selection toggler and routes through the same onToggle as checkbox clicks. Tests: SelectionTree.keyboard.dom.test.tsx modeled line-for-line on DropdownListbox.keyboard.dom.test.tsx (jsdom pragma, scrollIntoView stub in beforeEach, act-wrapped keyDown, activeElement assertions); a deferred browse promise drives the loading-focus case; assertions read aria attributes off the focused element.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/components/SelectionTree.keyboard.dom.test.tsx</automated>
  </verify>
  <done>The full key map works with focus assertions, geometry is uniform across lazy nodes, notice rows are outside the counts, Space is the only toggler and is idempotent, and exactly one treeitem is tabbable at a time</done>
  <acceptance_criteria>
    - SelectionTree.keyboard.dom.test.tsx exits 0 covering every key in the UI-SPEC table plus the three no-op cases (Right on expanded, Right while loading, Left on closed root) and the Space-twice flat-list idempotency
    - The file stubs Element.prototype.scrollIntoView in beforeEach and asserts document.activeElement for focus moves
    - SelectionTree.tsx renders aria-level, aria-setsize, aria-posinset, aria-expanded, and aria-checked on every treeitem; no treeitem carries aria-selected; exactly one treeitem has tabIndex 0
    - Notice rows (folder.none, folders.truncatedList, no-access, loading) expose no treeitem role or geometry attributes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INTEG-01 closure — sub-include absorption, expansion restore, panel-lifetime state, integration test</name>
  <files>web/src/lib/selectionTree.ts, web/src/lib/selectionTree.test.ts, web/src/pages/Containers.tsx, web/src/pages/Containers.tree.dom.test.tsx</files>
  <read_first>
    - internal/api/service.go lines 3699-3705 (MountInfo) and 3755-3782 (ContainerMounts class split — the exact-mount-root match rule that puts sub-mount includes into custom[])
    - web/src/lib/selectionTree.ts (whole — isStrictlyUnder for the partition; loadExpanded/saveExpanded from plan 01)
    - web/src/pages/Containers.tsx lines 696-930 (FoldersEditor as landed: mirror above the null return, cache ref, custom rows rendering)
    - web/src/pages/Containers.excludesAssistant.dom.test.tsx lines 1-80 (the FoldersEditor import-after-mock harness to copy for the integration file)
    - 02-RESEARCH.md Pitfall 3 (state survives close), Open Questions Q1/Q2 — in context above
    - 02-UI-SPEC.md Interaction Contract section Expansion persistence (D-05) — in context above
  </read_first>
  <behavior>
    - partitionCustomPaths(["/mnt/user/appdata/plex/Media", "/mnt/user/other"], ["/mnt/user/appdata/plex", "/mnt/user/media"]) returns underMount ["/mnt/user/appdata/plex/Media"] and standalone ["/mnt/user/other"] — segment-aligned (a sibling like /mnt/user/appdata/plex2 stays standalone; an exact mount-root match is never treated as sub-include)
    - FoldersEditor with a stored sub-include under a reachable mount renders that mount root mixed (whitelist start-state), the sub-include checked once expanded, and does NOT render a duplicate custom row for the same path
    - Expanding nodes, closing the FoldersEditor section, and reopening it: the same nodes are expanded (restored from the bv-tree-expanded-{name} key), children render from the editor-lifetime cache with no additional browse calls, and checkbox states match the (I, E) mirror
    - Expanding more than 64 nodes persists only the 64 most recent expansions (cap holds through the component path)
    - Integration: unchecking a subfolder under a checked mount PATCHes a body containing the bare mount source and the "!"+subfolder host path with selectionSource "tree"; unchecking the item's last include fires NO request (D-04 through the real FoldersEditor, not just the tree)
    - The mounts fetch happens once per FoldersEditor lifetime (loaded guard unchanged); reopen does not refetch mounts (component persistence kept, RESEARCH Q2)
  </behavior>
  <action>
    selectionTree.ts: add partitionCustomPaths(customPaths, mountSources) — a custom path is underMount iff it isStrictlyUnder some mount source (exact mount-root equality is NOT a sub-include; that path is the mount row itself); table tests per the behavior block including the plex-vs-plex2 segment case. Containers.tsx FoldersEditor: derive the rendered custom rows from partitionCustomPaths(custom paths, reachable mount sources) — standalone entries render as level-1 roots as today; underMount entries join the includes set for tree-state derivation under their mount (the mount root then classifies mixed per the whitelist start-state) and are filtered from the custom row list — one presentation per path (D-02; RESEARCH Q1 recommendation). Reachable filter: only m.reachable mounts absorb. Expansion persistence: SelectionTree reads loadExpanded(containerName) on mount to mark expanded nodes and calls saveExpanded on every expand/collapse (the helpers and cap landed in plan 01 — this wires them); selection never reads the key. Panel-lifetime: verify the mirror, listings cache, and translation roots still live above the null return so a section close/reopen restores without refetch (Pitfall 3). New file Containers.tree.dom.test.tsx: the excludesAssistant harness shape (vi.mock ../lib/api, FoldersEditor imported after the mock, providers wrapped), with getContainerMounts serving mounts + custom + excluded + roots and setBackupPaths recording bodies; cover the behavior block's integration cases including the D-04 no-call through the real editor and the close/reopen restoration.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/pages/Containers.tree.dom.test.tsx src/lib/selectionTree.test.ts</automated>
  </verify>
  <done>Sub-includes render once (inside the tree, mount mixed), custom rows show only standalone paths, expansion restores from localStorage within the cap, reopen reuses the cache without refetch, and the FoldersEditor integration test pins the wire contract and the D-04 no-call</done>
  <acceptance_criteria>
    - selectionTree.ts exports partitionCustomPaths with the segment-aligned under-mount rule; selectionTree.test.ts tables cover under-mount, exact-root non-match, and sibling-prefix non-match
    - Containers.tree.dom.test.tsx exits 0 covering: PATCH body with bare + "!"-prefixed host paths and selectionSource "tree", D-04 no-call through FoldersEditor, absorption single-presentation rendering, close/reopen expansion + cache restoration
    - FoldersEditor custom rows derive from partitionCustomPaths output; no path renders as both a custom row and a tree node
    - SelectionTree reads and writes expansion through loadExpanded/saveExpanded; selection state never reads the localStorage key
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite, lint, build, committed web/dist, Go regression</name>
  <files>web/dist</files>
  <read_first>
    - web/dist/index.html (placeholder sanity — the embed entry)
    - CLAUDE.md Build/test/lint section (the chain this task runs) — in context above
  </read_first>
  <action>
    Run the phase gate in order from the repo root: cd web && npm ci (lockfile-fresh install), npm test (whole vitest suite — selectionTree tables, SelectionTree dom + keyboard, Containers integration, i18n parity/quality/orphans across all 42 locales, every pre-existing test), npm run lint (eslint + the 8 bombvault/* house rules — declare any genuine exception in eslint.config.js, never a disable comment), npm run build (tsc --noEmit && vite build). Then commit the rebuilt web/dist (the SPA is embedded via web/embed.go; a stale dist means the binary ships the old panel). Then the Go regression from the repo root: go build ./... && go vet ./... and gofmt -l . printing nothing (no Go files changed this phase — this proves the embed still compiles and nothing drifted). Do NOT run the full go test ./... locally (restic is not on the Windows PATH; CI is the arbiter per the documented repo caveat) — note that in the summary.
  </action>
  <verify>
    <automated>cd web && npm test && npm run lint && npm run build && cd .. && go build ./... && go vet ./... && test -z "$(gofmt -l .)" && git status --porcelain web/dist | grep -qv '^$'</automated>
  </verify>
  <done>Whole web suite green, lint clean, production build green, web/dist rebuilt and committed, Go build/vet/gofmt clean — the phase is ready for /gsd-verify-work</done>
  <acceptance_criteria>
    - cd web && npm test exits 0 (full suite)
    - cd web && npm run lint exits 0
    - cd web && npm run build exits 0 and emits fresh web/dist assets
    - go build ./... && go vet ./... exit 0; gofmt -l . prints nothing
    - git status shows web/dist changes committed (not left dirty)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage expansion key -> focus/expansion model | Parsed JSON drives which nodes render expanded (never what is selected or fetched beyond cached paths) |
| Keyboard events -> selection toggles | Space is an alternative input path into the same PATCH boundary as clicks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | localStorage-parsed expansion entries | low | mitigate | Guarded JSON parse (plan 01 helpers); entries only match rendered node host paths; unknown entries are inert — never fetched, never PATCHed, never promoted into the selection mirror |
| T-02-10 | Tampering (user intent) | keyboard Space path bypassing the D-04 block | medium | mitigate | Space routes through the identical onToggle pipeline as checkbox clicks — one toggle semantics, one guard, one queue; the keyboard test asserts Space twice returns the prior flat list and the integration test pins the no-call block |
| T-02-11 | Information Disclosure | absorbed sub-include rendering (paths from mounts response) | low | accept | Paths are server-served host paths already shown in the panel today; rendering moves them between two views of the same response, no new data source |
| T-02-SC | Tampering | package installs | low | accept | Zero installs this phase; npm ci reinstalls the committed lockfile only |
</threat_model>

<verification>
- cd web && npm test — whole suite green (unit + dom + keyboard + integration + i18n across 42 locales)
- cd web && npm run lint — house rules green
- cd web && npm run build — tsc --noEmit && vite build green; web/dist committed
- go build ./... && go vet ./... && gofmt -l . — clean (embed proof; full go test deferred to CI per the Windows restic caveat)
- Manual smoke before /gsd-verify-work (optional): keyboard-only walk of the tree (Tab into it, arrows/Space), reopen the panel, uncheck the last folder — blocked inline with the translated message
</verification>

<success_criteria>
1. Success criterion 4 delivered: full keyboard operation with ARIA tree/checkbox roles (aria-expanded, aria-checked mixed, level/setsize/posinset on lazy nodes) — keyboard test file green
2. INTEG-01 complete: every discovered mount and custom path unfolds in the container panel; sub-includes present exactly once; integration test pins the wire contract
3. TREE-04 complete: reopen reconstructs selection from the server response exactly (integration test) while expansion restores as comfort state (D-05)
4. Phase gate: web/dist rebuilt and committed so the shipped binary embeds the new panel
</success_criteria>

## Flagged assumptions

- INTEG-01 edge-probe classification returned `unclassified` (#1110 — flagged assumption, never auto-resolved). Planned from INTEG-01's requirement text + D-02/D-03/D-04 + RESEARCH Pattern 3 and pinned by Containers.tree.dom.test.tsx (roots expandable, PATCH body, no-call guard, single presentation). Residual interpretation risk surfaced here rather than silently resolved.
- RESEARCH Q1 (where sub-mount includes render) is resolved by the recommended option — absorb into the tree, filter from the custom row list — which the UI-SPEC already adopted as the [auto] default; a pre-existing deployment's custom-row list therefore shrinks for sub-mount includes (they move under their mount row). Behavior-only change over the same stored set; flagged here for visibility at verification.
- RESEARCH Q2 (reopen refetch) resolved as component persistence (no forced mounts refetch on section reopen; page reload refetches) — consistent with every sibling editor.

---
phase: 02-container-panel-tree-selection
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - web/src/components/SelectionTree.tsx
  - web/src/components/SelectionTree.dom.test.tsx
  - web/src/pages/Containers.tsx
autonomous: true
requirements: [TREE-06, TREE-02, INTEG-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - 'An unreadable directory renders a muted no-access child row: the server-scrubbed error text verbatim (folder.couldNotRead fallback) plus a compact neutral folders.retry Button that re-enters the in-flight state — visually distinct from a plain empty directory (ok:true with zero dirs renders the folder.none row), with selection state untouched (TREE-06)'
    - 'No infinite spinners and no silent collapses: every browse outcome (including ok:false for restricted/missing/error and rejected promises) settles the per-node state in a finally; no code path treats an error response as an empty listing'
    - 'truncated:true renders the served children then one muted non-interactive folders.truncatedList notice row ("First 500 entries shown") at the end of the node''s list — never a treeitem, never counted in setsize/posinset counts, no pagination (D-06)'
    - 'Rapid toggles or key presses during an in-flight save are serialized by the one-deep PATCH queue: a further toggle while a save is in flight updates the mirror and marks dirty, and the latest full list is sent once on resolve; revert is re-derived from the latest mirror, so a slow or failing save never clobbers a newer toggle (pinned by a dom test firing two toggles back-to-back with the first reply failing)'
    - 'A toggle that would leave zero includes for the item is blocked before any PATCH: counting includes across the WHOLE item (all mounts plus custom paths), the row shows the inline text-xs text-statusWarn folders.emptySelectionBlocked line and replays glim-shake, and no request is sent (D-04); the server''s code:"empty-selection" envelope stays an unreachable backstop handled defensively — toast the error verbatim and revert — if it ever fires'
    - 'Guard and error messages wrap as normal inline text — user-facing copy is never truncated (long-text rule for E3/E4 rows)'
  artifacts:
    - path: web/src/components/SelectionTree.tsx
      provides: "complete per-node outcome rendering: loading, empty, truncated notice (D-06), no-access + retry (TREE-06), children; and the D-04 blocked-row inline line + shake trigger wired through onToggle result reporting"
    - path: web/src/components/SelectionTree.dom.test.tsx
      provides: "dom tests for the status trio rows, truncated notice non-interactivity, retry re-entry, D-04 block-before-PATCH, and the two-rapid-toggles queue ordering"
    - path: web/src/pages/Containers.tsx
      provides: "FoldersEditor persist hardening: one-deep serialized PATCH queue (dirty flag, send-latest-once on resolve), revert re-derived from the latest mirror, defensive handling of the coded empty-selection envelope"
  key_links:
    - from: web/src/components/SelectionTree.tsx (row rendering)
      to: web/src/lib/api.ts (BrowseResponse.status / truncated)
      via: "status trio drives the no-access row variant; truncated flag drives the notice row; ok:false is a distinct state from ok:true-with-zero-dirs"
      pattern: "status"
    - from: web/src/pages/Containers.tsx (persist queue)
      to: web/src/lib/api.ts (setBackupPaths)
      via: "in-flight guard + dirty flag; only the final full flat list is sent per drain"
      pattern: "selectionSource"
  prohibitions:
    - statement: "A zero-include toggle must never silently fall through to the empty-list auto-detection fallback: the client block fires before any request (D-04); full-deselect semantics are Phase 3 (INTEG-04), never smuggled in here"
      status: unverified
      flagged: true
    - statement: "An unreadable or vanished folder must never block saving a selection: listing failures render a row and leave the (I, E) mirror untouched — selection persistence and browse errors stay independent"
      status: unverified
      flagged: true
---

<objective>
Node outcome states and save-flow guard: every browse outcome renders honestly (TREE-06 no-access rows with retry, D-06 truncated notice, empty vs loading distinct), and the live-save flow gains the D-04 pre-PATCH zero-include block plus the serialized one-deep PATCH queue so rapid toggles can never clobber each other or silently deselect an entire container.

Purpose: closes the interim window left by plan 01 (unblocked last-include uncheck; await-per-save revert races) and delivers success criterion 5 (unreadable vs empty distinct, no hangs, no silent collapses). The client block is the only guard on the exclusions-only path — the server guard fires only on a fully-empty list, so this plan is the D-04 contract.
Output: hardened SelectionTree row states, FoldersEditor persist queue + guard, extended dom tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-container-panel-tree-selection/02-CONTEXT.md
@.planning/phases/02-container-panel-tree-selection/02-RESEARCH.md
@.planning/phases/02-container-panel-tree-selection/02-PATTERNS.md
@.planning/phases/02-container-panel-tree-selection/02-UI-SPEC.md
@.planning/phases/02-container-panel-tree-selection/02-01-SUMMARY.md

Decision key: D-04 pre-Phase-3 empty guard (client-side block, translated inline message routing to item deactivation); D-06 truncated listing notice row. RESEARCH Pitfalls 4 (dormant exclusions reach the server as explicit-none — the client block is the only guard), 5 (rapid toggles vs in-flight saves), 6 (infinite spinner / silent collapse).
</context>

## Artifacts this phase produces (this plan's share)

- `web/src/components/SelectionTree.tsx` — edited: per-node render-state machine gains the error/truncated/blocked variants; no new exports (internal state types only)
- `web/src/pages/Containers.tsx` — edited: FoldersEditor persist internals (queue state ref, dirty flag, revert re-derivation, blocked-toggle reporting); no exported-signature changes
- `web/src/components/SelectionTree.dom.test.tsx` — edited: new describe blocks for TREE-06/D-06/D-04/queue behaviors
- No new files; all 4 i18n keys already landed by plan 01

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TREE-06 + D-06 — no-access rows with retry, truncated notice, distinct from empty</name>
  <files>web/src/components/SelectionTree.tsx, web/src/components/SelectionTree.dom.test.tsx</files>
  <read_first>
    - web/src/components/SelectionTree.tsx (whole — as landed by plan 01: per-node fetch, loading/empty/error-rendering skeleton)
    - web/src/components/SelectionTree.dom.test.tsx (whole — harness to extend)
    - web/src/components/FolderBrowser.tsx lines 92-111 (ok:false handling precedent) and 204 (inline error paragraph — why list errors stay inline, never toasts; comment at 80-85)
    - internal/api/handlers.go lines 4191-4200 (classifyReadDirError — restricted/missing/error kinds) and 4328-4335 (truncated field)
    - 02-UI-SPEC.md sections Interaction Contract ("Lazy loading states per node" table) and Copywriting (error/truncated copy + reuse rules) — in context above
    - web/src/components/Button.tsx (the shared neutral control for the compact retry affordance)
  </read_first>
  <behavior>
    - browse replying ok:false with status "restricted" and an error string renders one muted child row containing that exact string verbatim plus a folders.retry button; the row is not a treeitem
    - browse replying ok:false without error text renders the folder.couldNotRead fallback string
    - clicking the retry button re-enters the in-flight state (spinner row) and, on a subsequent ok:true reply, renders the children — the failed cache entry is not reused
    - browse replying ok:true with truncated:true and N dirs renders the N child rows followed by exactly one muted non-interactive folders.truncatedList row; the notice row has no treeitem role and no checkbox
    - browse replying ok:true with zero dirs renders the folder.none row — asserted distinct (different text) from the error row and from the loading row in the same test
    - a rejected browse promise settles to the error row in every case — the spinner node count in the DOM is zero after settle (no infinite spinner)
    - toggling the checkbox of a node whose children failed to load still PATCHes normally (selection untouched by listing errors)
  </behavior>
  <action>
    Extend the per-node render states in SelectionTree.tsx per the UI-SPEC "Lazy loading states per node" table (behavior block is the pin). The error row: muted text-carbon-textMuted child row with the server-scrubbed text verbatim (res.error fallback t("folder.couldNotRead")) and a compact neutral Button labeled t("folders.retry") that clears the failed per-node state and refetches through the cache (plan 01 removes rejected promises from the cache — verify that holds; a failed entry must refetch, not replay). Keep listing errors inline in the tree (FolderBrowser's own comment explains why list-load errors are never toasts). The truncated row: after the served children, one muted non-interactive row with t("folders.truncatedList"), rendered OUTSIDE the treeitem count — a plain text row, no role, no pointer target, excluded from any setsize/posinset arithmetic (geometry lands plan 03; keep the row structurally separate now). Status colors stay off controls: the no-access and truncated rows are muted TEXT, never a status-colored control or badge (lint rule no-status-color-on-control). Wrap rule: guard and error messages are plain wrapping inline text — no truncate class on message rows (only node name spans truncate, from plan 01). Tests per the behavior block, in the existing vi.mock harness (browse replies shifted per test; assert rendered text, roles absent/present, and zero spinner nodes after settle).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/components/SelectionTree.dom.test.tsx</automated>
  </verify>
  <done>All five browse outcomes (loading, ok+children, ok+truncated, ok+empty, ok:false trio) render distinctly and settle in finally; retry refetches; truncated notice is a non-treeitem; selection toggles ignore listing errors</done>
  <acceptance_criteria>
    - SelectionTree.dom.test.tsx exits 0 with cases asserting: restricted-status verbatim text row, couldNotRead fallback, retry re-entry and refetch, truncated notice after children with no treeitem role on it, folder.none distinct from error and loading, zero spinner nodes after a rejected promise
    - SelectionTree.tsx renders the truncated notice and no-access rows outside the treeitem structure; neither carries a checkbox
    - No status-color utility class appears on the checkbox, chevron, or retry control (muted text rows only)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 client-side block + serialized one-deep PATCH queue</name>
  <files>web/src/pages/Containers.tsx, web/src/components/SelectionTree.tsx, web/src/components/SelectionTree.dom.test.tsx</files>
  <read_first>
    - web/src/pages/Containers.tsx lines 696-930 (FoldersEditor as landed by plan 01: mirror state, persist fn, onToggle path, shake technique at 843-844)
    - internal/api/service.go lines 3868-3872 (guard conditions — fires ONLY on a fully-empty normalized list with source "tree"; exclusions-only saves pass) and handlers.go lines 1152-1154 (the coded empty-selection refusal this plan makes unreachable)
    - web/src/lib/selectionTree.ts (applyToggle + toFlatList — the block decision reads the post-toggle includes set)
    - 02-RESEARCH.md Pitfalls 4 and 5 + Open Question 4 (the one-deep queue recommendation) and Pattern 6 (live-save with revert+shake, extended) — in context above
    - 02-UI-SPEC.md Interaction Contract sections "Save flow (D-03) and the D-04 guard" — in context above
  </read_first>
  <behavior>
    - Toggling off the LAST include of an item (one selected mount, no custom paths) fires NO setBackupPaths call; the row shows the folders.emptySelectionBlocked inline line styled text-xs text-statusWarn directly under the blocked row and replays glim-shake on that row
    - The blocked toggle leaves the (I, E) mirror unchanged (the checkbox state does not flip)
    - Toggling off the last MOUNT include while a custom path include exists PATCHes normally (the count spans the whole item)
    - Two rapid toggles where the first save replies ok:false: the second toggle's state survives — the final mirror equals the two-toggle result and the last PATCH body carries the final flat list (revert re-derived, never a captured pre-toggle snapshot)
    - Two rapid toggles with both saves succeeding: exactly one additional PATCH drains after the in-flight one resolves (dirty flag collapses the burst) and bodies are ordered
    - A reply envelope carrying ok:false with code "empty-selection" (unreachable backstop) toasts the server error text verbatim and reverts the mirror
  </behavior>
  <action>
    FoldersEditor persist (Containers.tsx): replace the await-per-toggle save with the one-deep serialized queue per RESEARCH Open Question 4 — a saving ref/state flag plus a dirty flag: on toggle, update the mirror optimistically; if a save is in flight, mark dirty and return; when the in-flight save resolves, if dirty, send the LATEST full flat list once and clear dirty. On failure, re-derive the revert from the current mirror plus the failed toggle's inverse (applyToggle semantics) rather than a captured snapshot, then replay glim-shake keyed by the toggled node path (row key includes the shake nonce). Defensive backstop: if the envelope reports ok:false with the coded empty-selection value, toast the server error verbatim and revert (this path should be unreachable once the block below exists — it is defense-in-depth, not the guard).

    D-04 block: in the toggle path, after computing the next (I, E) via applyToggle and BEFORE building any request, count includes across the whole item — the next includes set IS the whole item (mounts plus custom all live in the one mirror, D-02 single tree). Empty next includes: do not PATCH, do not apply the mirror change; instead report the block to the tree (a blocked-path signal or the shake/notice state keyed by node host path) so SelectionTree renders the inline t("folders.emptySelectionBlocked") line styled text-xs text-statusWarn directly under the blocked row plus one glim-shake replay on that row. The message text names the real alternative control (Include in schedule) per the pinned copy — no new controls are added by this guard (full-deselect semantics are Phase 3, INTEG-04).

    Tests per the behavior block in the existing harness: setBackupPaths mock records bodies and call order; a deferred promise controls the in-flight save timing for the queue cases; the blocked case asserts the mock was NOT called (call count unchanged) and the inline line is rendered.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/components/SelectionTree.dom.test.tsx && npm test</automated>
  </verify>
  <done>The last-include toggle never reaches the network (inline translated block + shake instead), the queue serializes bursts with send-latest-once, revert survives concurrent toggles, and the full web suite stays green</done>
  <acceptance_criteria>
    - SelectionTree.dom.test.tsx exits 0 with cases: no-call on last-include uncheck, mirror unchanged when blocked, custom-include-present case PATCHes, first-save-fails two-toggle case ends at the two-toggle mirror with the final body, burst-collapse case sends exactly one draining PATCH, coded empty-selection reply toasts and reverts
    - Containers.tsx persist contains the in-flight + dirty-flag queue structure; no await-per-toggle save path remains for tree toggles
    - The blocked-row inline line uses text-statusWarn text-xs classes and the folders.emptySelectionBlocked key; the row replays glim-shake via the nonce-in-key technique
    - cd web && npm test exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server error/status payloads -> rendered rows | Browse error strings and the coded envelope cross into the DOM |
| Client guard -> PATCH boundary | The D-04 block is the only guard on the exclusions-only save path (server guard fires only on fully-empty lists) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (user intent) | D-04 guard bypass (exclusions-only explicit-none) | high | mitigate | Block counts includes across the whole item BEFORE any request and never applies the mirror change when zero includes would remain; the server coded refusal is handled defensively (toast verbatim + revert); dom test pins that no request is sent |
| T-02-06 | Tampering (user intent) | revert race clobbering newer toggles | medium | mitigate | One-deep queue with dirty flag sends only the latest full list; revert re-derives from the live mirror; pinned by the two-toggle failing-first-save dom test |
| T-02-07 | Information Disclosure | error/status text rendering (no-access rows) | low | accept | Server text is already scrubbed (paths then credentials, Phase 1) and shown verbatim per house rule; the client adds no path formatting of its own |
| T-02-08 | DoS (client) | rapid-toggle PATCH storms | low | mitigate | The dirty-flag queue collapses bursts to one draining request per in-flight save |
</threat_model>

<verification>
- cd web && npm test — full suite green (new TREE-06/D-06/D-04/queue cases + everything from plan 01 unmodified)
- cd web && npm run lint — house rules green
- Interim window from plan 01 SUMMARY is closed: re-check that unchecking the last include now produces no network call
</verification>

<success_criteria>
1. Success criterion 5 delivered: unreadable vs empty directories distinguished per node (muted no-access row + retry vs plain empty row), no infinite spinners, no silent collapses
2. D-04 delivered: zero-include toggles blocked client-side with a translated inline message routing to item deactivation; the server refusal is an unreachable backstop
3. Save flow hardened: serialized queue, latest-wins drain, revert safe under concurrency (RESEARCH Pitfalls 4/5 closed)
</success_criteria>

## Flagged assumptions

- TREE-06 edge-probe classification returned `unclassified` (#1110 — flagged assumption, never auto-resolved). Planned from TREE-06's requirement text + D-06 + the UI-SPEC "Lazy loading states per node" table and pinned by the dom tests (status trio rows, retry re-entry, settle-in-finally, spinner-count-zero assertions). Residual interpretation risk surfaced here rather than silently resolved.

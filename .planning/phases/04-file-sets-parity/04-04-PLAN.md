---
phase: 04-file-sets-parity
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - web/src/pages/Files.tsx
  - web/src/pages/Files.tree.dom.test.tsx
autonomous: true
requirements: [INTEG-02]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Each expanded set shows a reviewable exclusions list under the root: the folders.exclusions count line and relative mono muted paths, collapsed on every reopen, rendered identically for active and dormant roots, and non-interactive — mutations flow only through the tree's one toggle pipeline (D-07, INTEG-03 pattern parity)"
    - "The exclusions disclosure is a plain button tabbable outside the roving tabindex (backupOrder precedent) and its open/close state is per-expansion component state, deliberately not persisted (audit view, not navigation comfort)"
    - "The file-set dialog renders files.pathChangeHint under the FolderBrowser so the path-edit clears-the-selection consequence is disclosed before it happens (A3 UX note; the key was budgeted in plan 03)"
    - "Phase gate is green: go build ./... and go vet ./... clean, gofmt -l . prints nothing, go test ./... passes (reduced locally where POSIX/restic tests skip by design; CI Lint+Test are the gate), the full web vitest suite passes, and cd web && npm run build (tsc --noEmit + vite build) succeeds"
    - "web/dist build artifacts stay uncommitted — only the tracked index.html placeholder remains, byte-identical (Phase 2/3 repo convention: real artifacts are gitignored; the production build is verified then discarded)"
  artifacts:
    - "web/src/pages/Files.tsx: the per-root exclusions disclosure (folders.exclusions count + rootExclusions list) inside the tree expansion, styled per the Phase 3 audit list"
    - "web/src/pages/Files.tsx: files.pathChangeHint muted line under the FolderBrowser in FileSetDialog"
    - "web/src/pages/Files.tree.dom.test.tsx: extended with the exclusions-disclosure pins (collapsed on reopen, non-interactive rows, identical for dormant roots) and the dialog hint pin"
  key_links:
    - "rootExclusions(root, exclusions) -> the audit list rows (D-07 list half; same classification source as the preview count)"
    - "FileSetDialog path field -> files.pathChangeHint disclosure -> the plan 02 PATCH-time clear rule it documents"
  prohibitions:
    - statement: "The exclusions list never becomes a mutation surface — no per-row remove, no ExcludesEditor fanout, no 'exclude this subfolder instead' affordance (Out of Scope: ExcludesEditor answers which files/globs, the tree answers which folders)"
      status: resolved
      verification: "the disclosure rows render no controls; the dom pin asserts the list is display-only and the only toggle path is the tree checkbox"
    - statement: "No container-only trust surface ports to files: no narrowing note (folders.narrowedNote), no Reset selection control (files has no auto-detection fallback to reset to), no CACHEDIR toggle"
      status: resolved
      verification: "grep of the Files.tsx diff for narrowedNote/resetSelection/cachedir returns nothing; the dom suite pins their absence"
    - statement: "web/dist build artifacts are never committed; the tracked placeholder is the only web/dist file in the commit"
      status: resolved
      verification: "git status --porcelain after the gate shows no web/dist changes to commit; web/dist/index.html is byte-identical to HEAD"
---

<objective>
**As a** BombVault user, **I want to** review which sub-folders a file set excludes and be warned that changing a set's folder clears its ticked selection, **so that** the selection stays legible after the fact and a path edit never silently discards my choice.

This plan completes the D-07 parity surfaces (the reviewable exclusions list riding `rootExclusions` — the same classification the preview count and the compile consume) and the A3 dialog disclosure, then runs the phase gate: the full Go chain, the full web suite, the production web build with the placeholder-preserving commit convention.

Purpose: trust surfaces close the loop opened by the tree (what will this set back up, and what did I carve out), and the gate proves the phase coheres before `/gsd-validate-phase`.
Output: audit disclosure + dialog hint + a green phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-file-sets-parity/04-CONTEXT.md
@.planning/phases/04-file-sets-parity/04-RESEARCH.md
@.planning/phases/04-file-sets-parity/04-UI-SPEC.md
@.planning/phases/04-file-sets-parity/04-03-SUMMARY.md
</context>

## Artifacts this phase-plan produces

- The per-root exclusions review disclosure in the Files.tsx tree expansion (folders.exclusions count line + rootExclusions-derived muted mono rows; plain button, collapsed by default on every reopen, non-persisted expansion state)
- The files.pathChangeHint muted line under the FolderBrowser in FileSetDialog
- Extended pins in web/src/pages/Files.tree.dom.test.tsx
- A green phase-gate record in the plan summary (commands + outcomes)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit surfaces — per-root exclusions review list (D-07) and the dialog path-change hint (A3)</name>
  <files>web/src/pages/Files.tsx, web/src/pages/Files.tree.dom.test.tsx</files>
  <read_first>
  - web/src/pages/Files.tsx (the plan 03 tree expansion — extend in place; the FileSetDialog + FolderBrowser area for the hint)
  - the Phase 3 exclusions list implementation in web/src/pages/Containers.tsx (the audit disclosure: plain button outside the roving set, folders.exclusions count, relative mono muted rows, non-persisted expansion) — the pattern to mirror exactly
  - web/src/lib/selectionTree.ts (rootExclusions — READ-ONLY reuse; relative path derivation for display)
  - web/src/lib/i18n.ts (folders.exclusions and the plan 03 files.pathChangeHint key — consume byte-identical, no new keys)
  - .planning/phases/04-file-sets-parity/04-UI-SPEC.md (Placement items 6 and the Copywriting Contract rows; the muted token assignments)
  - .planning/STATE.md (Phase 3 decisions: audit view not persisted, identical rendering for active and dormant roots, existence-unfiltered listing)
  </read_first>
  <behavior>
  - Test: a set with stored exclusions renders the disclosure count ("{n} exclusions") and expanding it lists each excluded branch as a relative mono muted row; rows contain no interactive controls.
  - Test: the disclosure is collapsed after collapse/re-expand of the set card (state not persisted) and renders for a dormant root (all-excluded) identically to an active one.
  - Test: the list is existence-unfiltered — a stored exclusion whose directory no longer exists still appears (Phase 3 A3 rule carried over; row-level warnings are the container panel's concern and are not re-implemented here beyond parity).
  - Test: FileSetDialog renders the files.pathChangeHint muted line under the FolderBrowser; sets without a Path keep the existing hint behavior untouched.
  </behavior>
  <action>
  1. Files.tsx: inside the plan 03 expansion, add the exclusions disclosure mirroring the Phase 3 Containers audit list — a plain button (not part of the tree's roving tabindex) with the folders.exclusions count, expanding to rootExclusions-derived rows (relative display paths, font-mono muted, dir=ltr, title attributes). Expansion state is component-local per expansion; nothing persisted; the section renders identically for active and dormant roots; zero exclusions render no disclosure at all.
  2. Files.tsx FileSetDialog: add the files.pathChangeHint muted caption under the FolderBrowser (the disclosure for plan 02's PATCH-time clear rule — the hint fires regardless of whether a selection exists; it states the consequence, not a condition).
  3. Extend Files.tree.dom.test.tsx with the behavior rows; assert the list rows contain no button/checkbox roles (the audit-only prohibition pin).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/pages/Files.tree.dom.test.tsx && npx tsc --noEmit && npx eslint src/pages/Files.tsx</automated>
  </verify>
  <done>Exclusions are reviewable after the fact exactly as in the container panel, the path-change consequence is disclosed in the dialog, and both are pinned display-only.</done>
  <acceptance_criteria>
  - grep -c "rootExclusions" web/src/pages/Files.tsx prints at least 1
  - grep -c "files.pathChangeHint\|pathChangeHint" web/src/pages/Files.tsx prints at least 1
  - cd web && npx vitest run src/pages/Files.tree.dom.test.tsx exits 0 including the non-interactive rows pin and the collapsed-on-reopen pin
  - npx tsc --noEmit exits 0; npx eslint src/pages/Files.tsx reports 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full Go chain, full web suite, production web build, placeholder-preserving commit check</name>
  <files></files>
  <read_first>
  - CLAUDE.md and .claude/CLAUDE.md (build/test/lint gates; the web build requirement and its nuance below)
  - .planning/STATE.md (the Phase 2 decision: web/dist real artifacts are gitignored — .gitignore lines 14-18 keep only the index.html placeholder so //go:embed compiles; the production build is verified locally and NOT committed; CI Lint+Test are the authoritative gates since restic and golangci-lint are not on this Windows box PATH)
  - justfile (the Go chain `just check` aggregates — just itself is absent locally, run the commands directly)
  </read_first>
  <action>
  1. Run the Go chain: go build ./... , go vet ./... , gofmt -l . (must print nothing), go test ./... (POSIX-only and restic-dependent tests skip by design on this box; any NON-skip failure is a phase defect to fix before proceeding).
  2. Run the web suite: cd web && npx vitest run (full run — Files, Containers, selectionTree, i18n gates, uiConventions).
  3. Run the production build: cd web && npm run build (tsc --noEmit && vite build) to prove the SPA compiles and bundles; then verify the working tree shows NO committable web/dist changes (artifacts are gitignored; web/dist/index.html must remain byte-identical to HEAD — restore it if the build touched it).
  4. Record every command + outcome in the plan summary; if any gate fails, fix and re-run before writing the summary. CI (Lint + Test + Build Docker Image) remains the authoritative gate on push.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && gofmt -l . && go test ./... && cd web && npx vitest run && npm run build && git status --porcelain -- web/dist</automated>
  </verify>
  <done>Every gate green; no web/dist artifact staged; the phase is coherent end-to-end (store → compile → boundary → editor → audit surfaces) and ready for /gsd-validate-phase.</done>
  <acceptance_criteria>
  - go build ./... exits 0; go vet ./... exits 0; gofmt -l . prints nothing
  - go test ./... exits 0 (skips allowed only for the documented POSIX/restic-skipped tests)
  - cd web && npx vitest run exits 0 (full suite)
  - cd web && npm run build exits 0
  - git status --porcelain -- web/dist prints nothing committable (placeholder byte-identical; artifacts ignored)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → API | no new boundary in this plan; the audit list and hint are read-only/disclosure surfaces over plan 02's validated state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Tampering (intent integrity) | the exclusions list becoming a second mutation path that bypasses the tree's normalization | high | mitigate | the list renders zero interactive controls; mutations flow only through the one toggle pipeline; pinned by the no-controls dom test |
| T-04-15 | Repudiation | a path edit silently discarding a selection the user does not know about | medium | mitigate | files.pathChangeHint disclosed in the dialog BEFORE the edit; the clear rule itself is server-enforced (plan 02) |
| T-04-SC | Tampering | package installs | low | accept | zero npm and Go installs this phase (04-RESEARCH.md Package Legitimacy Audit: none); no legitimacy checkpoint applies |
</threat_model>

<verification>
- Full local gate green per Task 2's commands; CI (Lint + Test + Build Docker Image) is the authoritative gate on push
- All four plans' pins coexist: plan 01's legacy argv pin, plan 02's boundary/refusal/restore pins, plan 03's editor pins, plan 04's audit pins — one `go test ./...` + one vitest run covering all
</verification>

<success_criteria>
- D-07 complete (preview count in plan 03 + exclusions list here), the A3 disclosure rendered, and the phase gate green
- INTEG-02 fully satisfied across the plan set: criterion 1 (plans 03-04) and criterion 2 (plans 01-02, snapshot-Paths proof at the argv level)
- The milestone's last phase is execution-ready with zero second implementations of selection semantics anywhere in the diff
</success_criteria>

<output>
Create `.planning/phases/04-file-sets-parity/04-04-SUMMARY.md` when done
</output>

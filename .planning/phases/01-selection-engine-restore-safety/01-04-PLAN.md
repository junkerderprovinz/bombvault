---
phase: 01-selection-engine-restore-safety
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "02-01"]
files_modified:
  - internal/api/service.go
  - internal/api/handlers.go
  - internal/api/handlers_test.go
  - internal/api/service_test.go
  - .planning/PROJECT.md
autonomous: true
requirements: [INTEG-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PATCHing an empty list with selectionSource:'tree' over a previously non-empty selection is refused with HTTP 200 {ok:false, error:<scrubbed>, code:'empty-selection'}"
    - "A legacy empty list (no selectionSource) still clears to auto-detection byte-for-byte (existing clients unchanged)"
    - "An exclusions-only save from the tree source is accepted: it IS the explicitly-deselected state, never refused"
    - "Unknown or absent selectionSource values are ignored (treated as legacy): no source value can fail a save"
    - "GET /api/containers/{name}/mounts renders stored exclusions in a new excluded array in host form and never as stale custom paths; Selected flags are computed from includes only"
    - "excluded is an empty slice (never null) when no exclusions are stored, matching the existing mounts/custom nil-guard style"
    - "PROJECT.md Key Decisions logs the positional-targets decision and the future-children allowlist semantic with outcomes recorded"
  artifacts:
    - path: internal/api/service.go
      provides: "errEmptySelection sentinel; SetBackupPaths selectionSource-aware empty-selection guard; ContainerMounts class split returning exclusions in host form"
    - path: internal/api/handlers.go
      provides: "SelectionSource optional pointer field on the PATCH body struct; codedFailEnvelope(err, code) helper; excluded field in handleContainerMounts response"
    - path: internal/api/handlers_test.go
      provides: "empty-selection guard contract tests (tree refusal, legacy compat, exclusions-only, unknown source) + mounts excluded-field contract tests"
    - path: .planning/PROJECT.md
      provides: "Key Decisions rows: positional-targets compilation + future-children allowlist semantic, outcomes recorded"
  key_links:
    - from: internal/api/handlers.go (handlePatchContainer)
      to: internal/api/service.go (errEmptySelection)
      via: "errors.Is classification routes the refusal to the coded envelope"
      pattern: "errEmptySelection"
    - from: internal/api/service.go (ContainerMounts)
      to: internal/api/handlers.go (handleContainerMounts excluded field)
      via: "class split returns exclusions separately; handler nil-guards and renders them host-form"
      pattern: "excluded"
  prohibitions:
    - statement: "The empty-selection guard never fires for legacy clients or non-tree sources; strictly source-gated, no heuristic sniffing of tree-shaped payloads"
      status: unverified
      flagged: true
    - statement: "selectionSource stays an optional wire field; omitting it never fails a save and no new mandatory wire field is introduced"
      status: unverified
      flagged: true
---

<objective>
Exclusion visibility + empty-deselect protection (the backend half of INTEG-04, which completes with documented UI semantics in Phase 3): stored exclusions become legible through the mounts endpoint instead of rendering as stale custom paths, and a tree-source deselect-everything is refused at the PATCH boundary with a machine-routable code instead of silently re-triggering the empty-list auto-detection fallback. Plus the phase bookkeeping the roadmap prescribes: log the positional-targets and future-children Key Decisions in PROJECT.md.

Purpose: D-03's "exclusions-only = explicitly deselected" state is only trustworthy if it is (a) visible and (b) guarded; and Phase 3's UI needs the `code:"empty-selection"` machine signal to route guidance.
Output: guarded PATCH + coded envelope; ContainerMounts class split + `excluded` wire field; PROJECT.md Key Decisions rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-selection-engine-restore-safety/01-CONTEXT.md
@.planning/phases/01-selection-engine-restore-safety/01-RESEARCH.md
@.planning/phases/01-selection-engine-restore-safety/01-PATTERNS.md
@.planning/phases/01-selection-engine-restore-safety/01-VALIDATION.md
@.planning/phases/01-selection-engine-restore-safety/01-01-SUMMARY.md

Decision key (used in task actions below; numbering follows 01-CONTEXT.md `<decisions>` order):
D-09..D-12 = INTEG-04 Q1-Q4 (no mandatory wire field, optional selectionSource:"tree" carrying intent; refusal in the house HTTP 200 envelope with code:"empty-selection"; generic helpers + containers wiring only in Phase 1; exit-from-all-deselected DEFERRED to Phase 3).
D-01..D-04 = encoding Q1-Q4 (plan 01, prerequisite). D-05..D-08 = browse (plan 02). D-13..D-16 = restore (plan 03).
</context>

## Artifacts this phase produces (this plan's share)

- `internal/api/service.go` — `errEmptySelection` sentinel (message-carrying, errors.Is-able); `SetBackupPaths(ctx, name, hostPaths []string, selectionSource string)` guard; `ContainerMounts` gains a third return (exclusions, host form)
- `internal/api/handlers.go` — `SelectionSource *string` body field; `codedFailEnvelope(err error, code string) map[string]any` next to failEnvelope; `"excluded"` additive response field
- (plans 01/02/03 add: selection helpers, browse contract, `mapRestorePaths`, `RestoreDeps.SkippedPaths`)

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — stored exclusions render as the mounts excluded array, not stale custom paths</name>
  <files>internal/api/service.go, internal/api/handlers.go, internal/api/handlers_test.go</files>
  <read_first>
    - internal/api/service.go lines 3680-3700 (toHostPath) and 3722-3764 (ContainerMounts: effective list, auto fallback, selSet exact-match, matched/custom loop at 3757-3762 with its existing nolint-with-reason style)
    - internal/api/handlers.go lines 1283-1307 (handleContainerMounts + the mounts/custom nil-guard pattern to copy)
    - internal/api/handlers_test.go — router harness region (newTestRouter family) and any existing ContainerMounts/mounts tests to extend
    - 01-PATTERNS.md sections "ContainerMounts split" and "handleContainerMounts additive field" (in context above) and 01-RESEARCH.md R2 (excluded shape) + Pitfall 3
  </read_first>
  <behavior>
    - Save ["/host/a/root", "!/host/a/root/branch"] then GET /api/containers/{name}/mounts: the root appears among selected mounts, the branch appears in a new top-level excluded array (host form), and NEITHER appears as a custom path
    - Save exclusions-only: the excluded array carries the entries; no stale custom paths with Exists:false are fabricated
    - Save a plain include list: excluded is [] (empty slice, not null) and behavior matches today
    - MountInfo.Selected is computed from includes only (an exclusion never marks a mount selected)
  </behavior>
  <action>
    Split classes in ContainerMounts (service.go:3722) per RESEARCH R4/Pitfall 3 and D-11 (generic helpers wired for containers in Phase 1; File Sets reuse the same helper in Phase 4): split the stored raw list via plan 01's helper (internal/api/selection.go SplitExclusion); includes drive everything that drives today (the auto fallback when empty, selSet exact-match, the custom loop for unmatched includes); exclusions bypass the custom loop entirely and are returned as a new third value already translated to host form via s.toHostPath of the bare container path. Signature becomes ContainerMounts(ctx, name) ([]MountInfo, []CustomPath, []string, error) — update the single production caller and any existing tests. MountInfo.Selected keeps its include-only semantics. Add a why-comment citing Pitfall 3 (a raw ! entry would otherwise render as a phantom custom path with Exists:false).

    In handleContainerMounts (handlers.go:1283) accept the third return and ADD "excluded" to the okEnvelope payload, nil-guarded to an empty slice exactly like the existing mounts/custom guards (never null). No other response field changes.

    Contract tests in internal/api/handlers_test.go through the real router (doJSON, assert m["ok"]): the three save-then-list cases above plus the include-only no-change case; assert the excluded entries arrive in HOST form and that custom contains no exclusion-derived entries.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/ -run 'TestContainerMounts|TestMountsExcluded' -count=1</automated>
  </verify>
  <done>The mounts endpoint renders exclusions as a first-class excluded array in host form; no stale custom paths; include-only responses are unchanged apart from the additive empty excluded field</done>
  <acceptance_criteria>
    - ContainerMounts returns three slices + error with exclusions already in host form (source assertion); SplitExclusion is used for the class split (no inline prefix logic — per D-02 the semantics stay in internal/api/selection.go)
    - go test ./internal/api/ -run 'TestMountsExcluded' -count=1 exits 0 covering mixed, exclusions-only, and include-only saves
    - The response payload key "excluded" is nil-guarded to [] (source assertion in handleContainerMounts)
    - Existing mounts tests (if any) pass with only the signature-change call-site update
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH empty-selection guard (coded envelope) + PROJECT.md Key Decisions</name>
  <files>internal/api/service.go, internal/api/handlers.go, internal/api/handlers_test.go, internal/api/service_test.go, .planning/PROJECT.md</files>
  <read_first>
    - internal/api/handlers.go lines 1090-1161 (handlePatchContainer body struct + SetBackupPaths call site) and lines 365-383 (decodeBody: DisallowUnknownFields makes the new field declaration MANDATORY) and lines 56-61 (failEnvelope — the coded helper goes beside it)
    - internal/api/service.go lines 3771-3800 (SetBackupPaths as plan 01 left it: split, translate, re-attach, normalize, store)
    - internal/store/targets.go GetTargetByContainer (for the prior-state read)
    - internal/api/handlers.go strOr helper (used for the optional string fields pattern)
    - .planning/PROJECT.md lines 67-74 (Key Decisions table — 3 rows currently pending)
    - 01-RESEARCH.md Open Questions 1-2 (strictly source-gated: no heuristic sniffing) and Common Pitfalls 7 (selectionSource must not break old clients)
  </read_first>
  <behavior>
    - Container has a non-empty stored selection; PATCH [] with selectionSource:"tree" returns ok:false with code:"empty-selection" and a scrubbed error; the stored selection is unchanged
    - Container has a non-empty stored selection; PATCH [] with NO selectionSource clears to auto-detection exactly as today
    - Container with any prior state; PATCH ["!/host/x/branch"] (exclusions-only) with selectionSource:"tree" succeeds
    - PATCH with selectionSource:"future-client-value" behaves exactly as if absent
    - A fresh container (no prior selection); PATCH [] with selectionSource:"tree" succeeds (nothing to protect)
  </behavior>
  <action>
    Add the optional wire field per D-09: SelectionSource *string with json tag "selectionSource" on the handlePatchContainer body struct — the declaration is MANDATORY because decodeBody runs DisallowUnknownFields and would otherwise reject every tree save that sends it; omitting it stays legal (pointer field). Pass strOr(body.SelectionSource) into a widened setter signature SetBackupPaths(ctx, name, hostPaths []string, selectionSource string) and update every existing call site/test (empty string = absent/legacy). Add a doc comment on the field noting only the literal value "tree" carries meaning, any other value is ignored (treated as absent), and that SPA and server ship in one binary so version skew is a non-issue (RESEARCH Pitfall 7).

    Implement the guard in service.SetBackupPaths per D-09/D-10: after normalization, when selectionSource == "tree" AND the normalized entry list is empty AND the previously stored raw SelectedPaths (store.GetTargetByContainer) is non-empty, return a new package-level sentinel errEmptySelection (message-carrying, errors.Is-able, message: an explicit empty selection would re-enable automatic appdata detection). Strictly source-gated per the research recommendation: no payload sniffing, legacy and unknown sources keep today's clears-to-auto-detect behavior byte-for-byte. An exclusions-only result is non-empty and passes (that IS the explicitly-deselected state — D-03). A fresh container with no prior selection has nothing to protect and passes.

    In handlePatchContainer's BackupPaths branch, classify with errors.Is(err, errEmptySelection) and respond via a new codedFailEnvelope(err error, code string) helper placed beside failEnvelope (handlers.go:56-61) returning {ok:false, error: scrubError(err), code: code} with code "empty-selection" (per D-10 the machine code lets the Phase 3 UI route guidance); all other errors keep the plain failEnvelope path. Everything stays in the HTTP 200 envelope.

    Phase bookkeeping (roadmap Notes prescription): in .planning/PROJECT.md Key Decisions, (a) add row "Selections compile to maximal-root restic positional targets; exclusions encode as !-prefixed entries in the same flat list" with rationale "restic excludes do not apply to positional sources — exclude-based encoding disqualified" and outcome "Decided (Phase 1)"; (b) add row "Future children of an included root are included (allowlist semantic)" with rationale "narrowing selections communicate the future-children allowlist semantic; UI communication lands with Phase 3 SELECT-03" and outcome "Accepted (Phase 1)"; (c) set the Outcome column of the existing row "Tree selection = new view over the existing flat backupPaths set" to "Decided (Phase 1)" and of the row "Lazy-load tree children from a server directory listing (extend /api/browse)" to "Backend landed (Phase 1)". Leave the ExcludesEditor coexistence row pending (later-phase concern).

    Tests: TestEmptySelectionGuard in internal/api/handlers_test.go covering all five behavior cases above (assert m["ok"] false and m["code"] == "empty-selection" on the refusal; assert the stored selection is untouched after a refusal), plus a service_test.go case that the widened signature keeps every plan-01 behavior identical when selectionSource is empty.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/ -run 'TestEmptySelectionGuard|TestSetBackupPaths|TestPatchContainer' -count=1 && go vet ./internal/api/ && test -z "$(gofmt -l internal/api)"</automated>
  </verify>
  <done>Tree-source deselect-everything is refused with the coded envelope and prior state preserved; legacy clears, exclusions-only saves, unknown sources, and fresh containers all behave exactly as specified; Key Decisions logged</done>
  <acceptance_criteria>
    - The PATCH body struct declares SelectionSource *string (source assertion — without it DisallowUnknownFields rejects tree payloads)
    - go test ./internal/api/ -run 'TestEmptySelectionGuard' -count=1 exits 0 covering all five behavior cases including byte-compat for the legacy empty list
    - codedFailEnvelope exists beside failEnvelope and the refusal carries code "empty-selection" in the HTTP 200 envelope
    - SetBackupPaths has the selectionSource string parameter with empty-string legacy semantics and all pre-existing call sites updated
    - .planning/PROJECT.md Key Decisions contains the positional-targets row and the future-children allowlist row with outcomes recorded
    - go vet ./internal/api/ clean; gofmt reports nothing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PATCH body -> selection semantics | selectionSource is untrusted input gating a destructive-intent refusal |
| Stored exclusions -> mounts response | Persisted prefixed strings cross to the SPA as host-form paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-13 | Tampering (user intent) | PATCH empty selection | high | mitigate | Tree-source empty list over a non-empty prior selection refused with code:"empty-selection"; prior state preserved on refusal; legacy path byte-compat tested |
| T-01-14 | Tampering | selectionSource spoofing/unknown values | low | mitigate | Only the literal "tree" carries meaning; every other value ignored (treated as absent) so no source value can fail a save; pointer field keeps omission legal |
| T-01-15 | Information Disclosure | mounts excluded field | low | mitigate | Exclusions rendered via the existing toHostPath translation (host form the caller submitted); no raw container-internal layout introduced; envelope unchanged otherwise |
| T-01-SC | Tampering | package installs | low | accept | No package installs this phase (stdlib + in-repo code only) |
</threat_model>

<verification>
- `go test ./internal/api/ -run 'TestEmptySelectionGuard|TestMountsExcluded|TestSetBackupPaths|TestContainerMounts' -count=1` green (quick sampling path from 01-VALIDATION.md)
- `go test ./... && go vet ./... && gofmt -l .` clean at plan end (wave merge gate)
- Full phase gate before /gsd-verify-work: entire suite green (CI: restic 0.17.3); `git status --porcelain web/` empty (no web/ changes in this phase)
</verification>

<success_criteria>
1. Fully deselecting a mount's tree through the tree source produces an explicit refusal, never a silent flip to auto-detection (INTEG-04 backend half; UI documentation completes in Phase 3)
2. Exclusions are reviewable machine-readably after the fact via the mounts endpoint's excluded array (read-side foundation for Phase 3's reviewable-exclusions UI)
3. The roadmap-prescribed Key Decisions (positional targets, future-children allowlist) are logged with outcomes
</success_criteria>

## Deferred (explicitly out of this plan — do not implement)
- Exit from the all-deselected state / explicit return to auto-detection (D-12 -> Phase 3, INTEG-04 UI)
- Narrowing note UI ("new snapshots will contain only the selected folders") -> Phase 3 (SELECT-03)
- Coverage diff at backup time -> v2 (SELECT-06); fanout "exclude this subfolder instead" -> follow-up

<assumption_delta_decision>
Noun: the single flat backupPaths selection set (store targets.selected_paths).
Decision: no-change — the guard ADDS a protection around the existing empty-means-auto-detection boundary without moving it; selectionSource is a transient intent signal on the request, never persisted as a second representation. The exclusions-only state remains an entry-class within the one list (per D-03), not a parallel model.
</assumption_delta_decision>

<output>
Create `.planning/phases/01-selection-engine-restore-safety/01-04-SUMMARY.md` when done
</output>

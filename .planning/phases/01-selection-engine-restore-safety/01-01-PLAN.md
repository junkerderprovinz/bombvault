---
phase: 01-selection-engine-restore-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/selection.go
  - internal/api/selection_test.go
  - internal/api/service.go
  - internal/store/targets.go
  - internal/api/service_test.go
autonomous: true
requirements: [SELECT-01, SELECT-02, SELECT-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Saving a mixed folder selection through the API stores the maximal-root form: kept parents, redundant descendant includes dropped, unchecked sub-branches below included roots stored as !-prefixed exclusions in the same flat list, and reading it back reproduces exactly which branches are included and excluded"
    - "A stored list containing both an ancestor and its descendant normalizes to the ancestor alone (per-class pruning: includes elide includes, excludes elide excludes; a root include plus a branch exclude coexist)"
    - "Normalization output is canonically ordered (sorted includes then sorted exclusions) so equal selections produce byte-identical stored sets; input order does not affect the result"
    - "Normalizing an already-normalized set returns it unchanged; normalizing an empty input yields empty"
    - "Sibling keep-lists (adjacent, non-nested) are all preserved (only strict descendants are elided); a pure include list (whitelist start-state) passes through the same normalizer with no special-casing"
    - "Existing prefix-free saved backupPaths keep working with zero migration: readers treat a prefix-free list as all-includes, an empty list still means exactly auto-detection at the persistence boundary, and no migration file is added"
    - "An exclusions-only stored list is never treated as data-gone: storedDataIsGone returns false for a non-empty raw list whose includes are empty, so an explicitly deselected container still runs (definition-only) instead of being refused as not reachable"
    - "A backup run after a narrowed selection hands restic exactly the maximal-root container-form positional paths and derives zero exclude flags from the selection (fakeResticEngine lastPaths / lastExcludes assertions)"
    - "Saves serialize through the owned store setter (store.SetBackupPaths): last completed write wins, readers never observe partial state"
  artifacts:
    - path: internal/api/selection.go
      provides: "ExclusionPrefix const, SplitExclusion, PruneMaximal, NormalizeSelection — pure prefix/selection semantics, no receiver, no store access"
    - path: internal/api/selection_test.go
      provides: "table-driven normalization/pruning/classification tests covering maximal roots, per-class pruning, orphan exclusions, whitelist start-state, canonical order, idempotency"
    - path: internal/api/service.go
      provides: "SetBackupPaths parses the exclusion prefix BEFORE toContainerPath and normalizes before store; configuredBackupPaths returns includes-only; storedDataIsGone classifies explicit-none"
    - path: internal/store/targets.go
      provides: "doc-comment sentence noting entries may carry the ! exclusion prefix (semantics owned by internal/api/selection.go) — zero functional change"
  key_links:
    - from: internal/api/service.go (SetBackupPaths)
      to: internal/api/selection.go (NormalizeSelection)
      via: "split prefix, translate bare, re-attach, normalize, then store.SetBackupPaths"
      pattern: "NormalizeSelection\\("
    - from: internal/api/service.go (configuredBackupPaths)
      to: internal/api/service.go (effectiveBackupPaths -> Backup -> BackupDeps.AppdataPaths)
      via: "includes-only list becomes the restic positional list"
      pattern: "effectiveBackupPaths"
  prohibitions:
    - statement: "No restic --exclude flag is ever derived from the folder selection; the selection compiles to maximal-root positional targets only (restic excludes do not apply to positional sources)"
      status: unverified
      flagged: true
    - statement: "The save path never silently repairs or drops stored entries; stale-path cleanup is the engine's job at run time (onlyExistingPaths), never the setter's"
      status: unverified
      flagged: true
    - statement: "store.Target AppdataPaths recorded at backup time never carries !-prefixed entries; it stays positional truth so the restore mapping input (plan 03) stays clean"
      status: unverified
      flagged: true
---

<objective>
Selection encoding keystone: tree selections made through the API normalize losslessly into the UNCHANGED flat `backupPaths` set (maximal includes + `!`-prefixed exclusions in the same list), the six readers classify the prefixed list correctly, and a backup after a narrowed selection hands restic exactly the maximal-root positional targets.

Purpose: this is the risky heart of the milestone (success criteria 1, 2, 3) — the maximal-roots <-> restic positional-targets translation must be proven end-to-end before any UI leans on it (Phase 2) and before restore hardening (plan 03) can map against clean positional truth.
Output: `internal/api/selection.go` + table tests, normalized `SetBackupPaths`, reader classification, backup-positional proof via `fakeResticEngine`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-selection-engine-restore-safety/01-CONTEXT.md
@.planning/phases/01-selection-engine-restore-safety/01-RESEARCH.md
@.planning/phases/01-selection-engine-restore-safety/01-PATTERNS.md
@.planning/phases/01-selection-engine-restore-safety/01-VALIDATION.md

Decision key (used in task actions below; numbering follows 01-CONTEXT.md `<decisions>` order):
D-01..D-04 = encoding Q1-Q4 (`!` prefix in the same flat list; pure helpers in selection.go; orphan exclusions meaningful; per-class pruning).
D-05..D-08 = browse Q1-Q4 (plans 02). D-09..D-12 = INTEG-04 Q1-Q4 (plan 04). D-13..D-16 = restore Q1-Q4 (plan 03).
</context>

## Artifacts this phase produces (this plan's share)

- `internal/api/selection.go` — exported: `ExclusionPrefix` (const, value `"!"`), `SplitExclusion(entry string) (bare string, excluded bool)`, `PruneMaximal(paths []string) []string`, `NormalizeSelection(entries []string) []string`
- `internal/api/selection_test.go` — NEW table-driven test file
- `internal/api/service.go` — edited: `SetBackupPaths`, `configuredBackupPaths`, `storedDataIsGone` (no signature change in this plan)
- `internal/store/targets.go` — doc comment only
- (plans 02/03/04 add: browse contract symbols, `mapRestorePaths`, `errEmptySelection`, mounts `excluded` field, `RestoreDeps.SkippedPaths`)

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — a mixed selection saves normalized into the flat set and reads back</name>
  <reversibility rating="one-way">The !-prefix encoding (per D-01) puts exclusion semantics into stored selected_paths string data; once deployments store prefixed entries, undoing the encoding is a data migration over every stored selection.</reversibility>
  <files>internal/api/selection.go, internal/api/selection_test.go, internal/api/service.go, internal/store/targets.go, internal/api/service_test.go</files>
  <read_first>
    - internal/paths/paths.go (whole file — house style for pure path helpers, the strict-ancestor primitive at lines 44-48, the POSIX-only note at lines 31-32)
    - internal/api/service.go lines 1130-1160 (toContainerPath) and 3765-3800 (SetBackupPaths current body)
    - internal/store/targets.go lines 20-40 (SelectedPaths doc comment) and 290-320 (SetBackupPaths owned setter)
    - internal/api/service_test.go lines 2100-2160 (existing SetBackupPaths + backup flow test to extend)
    - internal/api/handlers_test.go lines 29-98 (newTestRouter family + doJSON helper)
    - 01-PATTERNS.md sections "selection.go (NEW)", "SetBackupPaths", "internal/store/targets.go" (in context above)
  </read_first>
  <behavior>
    - NormalizeSelection(["/c/appdata/plex", "/c/appdata/plex/config", "!/c/appdata/plex/transcoding", "!/c/appdata/plex/transcoding/cache"]) returns ["/c/appdata/plex", "!/c/appdata/plex/transcoding"] (include elides include-descendant; exclusion elides exclusion-descendant; classes coexist)
    - NormalizeSelection(["!/c/appdata/plex/transcoding"]) returns ["!/c/appdata/plex/transcoding"] (orphan exclusion preserved — the explicit-none carrier)
    - NormalizeSelection(["/c/appdata/plex/config", "/c/appdata/plex"]) returns ["/c/appdata/plex"] (whitelist start-state through the same normalizer)
    - NormalizeSelection([]) returns []; NormalizeSelection(normalized input) returns it unchanged; two sibling includes both survive; output is sorted includes then sorted exclusions regardless of input order
    - SplitExclusion("!/mnt/x") returns ("/mnt/x", true); SplitExclusion("/mnt/x") returns ("/mnt/x", false)
    - PATCH /api/containers/{name} with a mixed host-path list through the real router returns the ok envelope and stores the container-form normalized list (includes bare, exclusions prefixed)
    - PATCH with a legacy prefix-free list stores it unchanged (all-includes); PATCH with [] stores [] (auto-detect boundary untouched)
  </behavior>
  <action>
    Create internal/api/selection.go as pure helpers per D-02 (CONTEXT encoding Q2): no receiver, no store access, no cfg — table-testable funcs on []string only. Export surface exactly as RESEARCH R3: const ExclusionPrefix = "!"; SplitExclusion(entry string) (bare string, excluded bool); PruneMaximal(paths []string) []string; NormalizeSelection(entries []string) []string. The strict-descendant test is strings.HasPrefix(child, ancestor+"/") on cleaned POSIX paths (reuse the paths.go:44-48 primitive verbatim as the conceptual base) — per D-04 pruning is PER-CLASS: an include elides only strictly-lower includes, an exclusion elides only strictly-lower exclusions, and include+exclude deliberately coexist (included root + excluded branch); per D-03 an orphan exclusion (no included ancestor) is preserved. NormalizeSelection canonicalizes a mixed, already-container-translated list: split classes, per-class PruneMaximal, dedupe, canonical order = includes sorted lexically then exclusions sorted lexically, each exclusion stored as ExclusionPrefix + container path. Mirror paths.go's package-doc style, why-comments citing the CONTEXT decisions, and the POSIX-only note (makes the prefix tests safe on the Windows dev box).

    Rewire service.SetBackupPaths (service.go:3771) keeping its existing shape per D-01/D-02: TrimSpace each entry; call SplitExclusion BEFORE toContainerPath (a raw !-prefixed host path fails toContainerPath's TrimPrefix test and would reject the whole save — RESEARCH Pitfall 1); translate only the bare path through s.toContainerPath keeping the existing rejection message for untranslatable paths; re-attach ExclusionPrefix to the container form; validate both classes identically (bare path must sit under the host mount; a bare "!" with empty path is rejected as an empty excluded path); skip empty entries as today; dedupe on the prefixed container path; run NormalizeSelection over the accumulated list before calling s.store.SetBackupPaths. Do NOT change the method signature in this plan (the selectionSource parameter arrives with the guard in plan 04).

    internal/store/targets.go: add ONE sentence to the SetBackupPaths doc comment noting entries may carry the ! exclusion prefix whose semantics are owned by internal/api/selection.go, and that the store treats them as opaque strings. Zero functional change anywhere in internal/store (SELECT-02: no migration, selected_paths JSON untouched, UpsertTarget still never resets the column).

    Tests: internal/api/selection_test.go — table-driven in the restic_args_test.go discipline (literal want slices + reflect.DeepEqual, comment citing the decision), covering every behavior case above plus dual-root translation (split-root AND identity-root, mirroring paths_test.go's dual-root framing). Extend internal/api/service_test.go (pattern at lines 2124-2147): a real-router round-trip via the newTestRouter + doJSON helpers — PATCH the mixed host list, assert the ok envelope (assert m["ok"], never status codes for domain outcomes), then store.GetTargetByContainer returns the normalized container-form list; PATCH a legacy prefix-free list round-trips unchanged; PATCH [] persists []. Pre-existing tests must pass unmodified.
  </action>
  <verify>
    <automated>go build ./... && go test ./internal/api/ -run 'TestNormalizeSelection|TestSplitExclusion|TestPruneMaximal|TestSetBackupPaths' -count=1 && git diff --exit-code -- internal/store/migrate.go</automated>
  </verify>
  <done>PATCHing a mixed host-path selection through the real router stores the normalized maximal-root form with prefixed exclusions; legacy prefix-free lists round-trip as all-includes; [] persists as [] (auto-detect); normalize tables green; migrate.go untouched</done>
  <acceptance_criteria>
    - internal/api/selection.go exists and exports exactly ExclusionPrefix, SplitExclusion, PruneMaximal, NormalizeSelection with the signatures above; the file contains no store reference and no method receiver
    - go test ./internal/api/ -run 'TestNormalizeSelection' -count=1 exits 0 with table cases for: include-elides-include-descendant, exclusion-elides-exclusion-descendant, include+exclude coexistence, orphan exclusion preserved, pure include passthrough, canonical ordering, empty input, idempotency
    - service.SetBackupPaths calls SplitExclusion before toContainerPath and NormalizeSelection before store.SetBackupPaths (source assertion in service.go)
    - The real-router PATCH round-trip test asserts the stored normalized form via store.GetTargetByContainer
    - go test ./internal/api/ -run 'TestSetBackupPaths' -count=1 exits 0 including the legacy prefix-free and empty-list cases
    - internal/store/migrate.go is not modified (SELECT-02 zero migration)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Readers classify the prefixed list; backups hand restic maximal-include positionals</name>
  <files>internal/api/service.go, internal/api/service_test.go</files>
  <read_first>
    - internal/api/service.go lines 3722-3764 (ContainerMounts — NOT edited here; plan 04 owns its class split), 3820-3900 (configuredBackupPaths, effectiveBackupPaths, emptyBackupIsUnreachable, storedDataIsGone + the #175/#181 doc paragraphs)
    - internal/api/service.go lines 4030-4070 (Backup: effective list, #181 guard, UpsertTarget AppdataPaths) and 4105-4120 (BackupDeps construction)
    - internal/api/excludes_suggest.go lines 1000-1015 (SuggestExcludes inherits configuredBackupPaths)
    - internal/backup/orchestrator.go lines 460-475 (definition-only skip when AppdataPaths is empty)
    - internal/api/service_test.go lines 3790-3835 (fakeResticEngine: lastPaths, lastExcludes, snaps)
  </read_first>
  <behavior>
    - configuredBackupPaths on a stored ["/x", "!/x/branch"] list returns ["/x"] (includes only); the explicit-vs-auto test stays on the RAW list (exclusions-only is non-empty = explicit)
    - storedDataIsGone on a non-empty raw list whose includes are empty returns false (explicit-none is never "data gone")
    - Backup after narrowing records eng.lastPaths == the maximal container-form includes and eng.lastExcludes unchanged
    - Backup for an exclusions-only container is NOT refused by the #181 unreachable guard; empty positionals flow to the existing definition-only orchestrator path
  </behavior>
  <action>
    configuredBackupPaths (service.go:3826) per the R4 discovery set: keep the explicit-vs-auto test on the RAW SelectedPaths list exactly as-is (an exclusions-only list is correctly non-empty = explicit), then split via SplitExclusion and return includes-only so every downstream consumer (effectiveBackupPaths, SuggestExcludes) inherits correct behavior. Preserve the existing #175 "why" paragraph verbatim; add a short why-comment noting raw-list test + includes-only return (exclusions never become positionals — locked position L1/L14).

    storedDataIsGone (service.go:3874): classify explicit-none per D-03 — when the raw SelectedPaths list is non-empty but its includes are empty, return false (the item is explicitly deselected, not unreachable); otherwise stat the includes as today. Preserve the full #181 doc paragraph (it documents the three-state decision this edit extends); extend it with one sentence for the explicit-none case. Warning sign being prevented (RESEARCH Pitfall 2): a container whose user deselected everything must never be refused as "not reachable".

    No change to Backup itself: positionals already flow from effectiveBackupPaths, and AppdataPaths recorded on the target (service.go:4063) therefore stays includes-only positional truth — never carries the prefix (keeps plan 03's restore mapping input clean). No change to ContainerMounts here (plan 04 owns the class split and the mounts wire field); its current behavior with prefixed entries is interim intra-phase state resolved in plan 04.

    Tests in internal/api/service_test.go: extend the existing SetBackupPaths + backup flow test pattern (lines 2124-2147) — (a) narrowed selection then Backup asserts eng.lastPaths equals the maximal container-form includes and eng.lastExcludes is unchanged (proves no exclude flag derives from the selection); (b) exclusions-only container: Backup proceeds (no unreachable refusal) with an empty positional list (definition-only path); (c) configuredBackupPaths unit cases: mixed stored list returns includes-only, exclusions-only raw list still counts as explicit (not auto-detect); (d) storedDataIsGone cases: raw non-empty + empty includes returns false; prefix-free legacy list behaves exactly as before.
  </action>
  <verify>
    <automated>go test ./internal/api/ -run 'TestBackupPaths|TestSetBackupPaths|TestStoredData|TestBackupNarrowed|TestConfiguredBackupPaths' -count=1 && go vet ./internal/api/ && gofmt -l internal/api internal/store && test -z "$(gofmt -l internal/api internal/store)"</automated>
  </verify>
  <done>Readers split the prefixed list correctly; the #181 guard classifies explicit-none as not-gone; a narrowed backup hands restic exactly the maximal-root includes with zero selection-derived excludes; legacy lists behave byte-identically</done>
  <acceptance_criteria>
    - configuredBackupPaths returns includes-only while keeping its explicit test on the raw list (source assertion)
    - storedDataIsGone returns false for a non-empty raw list whose includes are empty (test)
    - go test ./internal/api/ -run 'TestBackupNarrowed' -count=1 exits 0 asserting eng.lastPaths equals the maximal includes and eng.lastExcludes is empty/unchanged
    - go test ./internal/api/ -run 'TestBackupPaths|TestStoredData' -count=1 exits 0 including exclusions-only and legacy-list cases
    - go vet ./internal/api/ and gofmt report nothing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PATCH body -> selection store | User-influenced path strings cross into persisted state and later into restic argv |
| Stored selection -> readers/backup | Persisted strings are re-parsed by six readers; a mis-classified entry inverts backup behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | service.SetBackupPaths | high | mitigate | Per-element containment re-check via toContainerPath on the BARE path of both classes; TrimSpace; reject bare "!" with empty path; prefix parsed before translation so no unvalidated string is stored |
| T-01-02 | Tampering | Backup -> restic argv | high | mitigate | Selection compiles to positionals only (after --, restic.go:381-382); no exclude flag is ever derived from the selection; eng.lastExcludes-unchanged assertion pins it |
| T-01-03 | Tampering (user intent) | storedDataIsGone / #181 guard | medium | mitigate | Explicit-none classification returns false so a deselected container is never refused as not reachable; table tests cover the three states |
| T-01-04 | Information Disclosure | SetBackupPaths error text | low | accept | Error echoes the caller's own submitted path (%q) — same-subject disclosure, existing house pattern, flows through failEnvelope scrubbing |
| T-01-SC | Tampering | package installs | low | accept | No package installs this phase — Go stdlib + in-repo code only (RESEARCH Package Legitimacy Audit: nothing to audit) |
</threat_model>

<verification>
- `go test ./internal/api/ -run 'TestSelect|TestBackupPaths|TestNormalize' -count=1` green (quick sampling path from 01-VALIDATION.md)
- `go test ./...` green (restic-dependent tests skip locally without the binary; full coverage on CI)
- `go build ./... && go vet ./... && gofmt -l .` clean; `git status --porcelain internal/store/migrate.go` empty (SELECT-02)
- Existing tests pass unmodified (no edits to existing want slices or existing browse/backup contract tests)
</verification>

<success_criteria>
1. A mixed selection saved through the API persists as maximal roots + `!` exclusions and reads back with classes intact (criteria 1)
2. A backup after narrowing passes restic exactly the maximal-root positionals, never exclude-based encoding (criterion 2)
3. Legacy prefix-free lists and `[]` behave byte-identically to today; zero migration (criterion 3 — engine half; the PATCH-boundary guard is plan 04)
</success_criteria>

## Flagged assumptions
- SELECT-02 edge-probe classification returned `unclassified` (#1110 — flagged assumption, never auto-resolved). The zero-migration semantics are planned from SELECT-02's requirement text + CONTEXT encoding Q1/Q3 and covered by the store round-trip tests (legacy prefix-free, `[]` = auto-detect, no migration file) plus the no-migration source check. Residual interpretation risk is surfaced here rather than silently resolved.

<assumption_delta_decision>
Noun: the single flat backupPaths selection set (store targets.selected_paths).
Decision: no-change — the detector fired on the prose phrase "the chosen snapshot" (restore-time snapshot choice), not on a modeling transition. The phase generalizes WITHIN the one existing list (entries may carry the ! prefix) and adds no second representation; SELECT-02 locks identity preservation. Invariant test encoding the generalized intent: the legacy/mixed/exclusions-only round-trip table in selection_test.go.
</assumption_delta_decision>

<output>
Create `.planning/phases/01-selection-engine-restore-safety/01-01-SUMMARY.md` when done
</output>

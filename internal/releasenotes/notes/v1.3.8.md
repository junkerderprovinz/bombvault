More UI tweaks from feedback.

## Changed
- **Sidebar order:** "Jobs" now sits directly under "Dashboard" (above Containers).
- **Sidebar logo:** dropped the white background tile. In dark mode the logo now gets a soft glow that lifts its dark areas off the dark sidebar while keeping all of its colours; light mode shows it plain.

Image: `ghcr.io/junkerderprovinz/bombvault:1.3.8` (also `:1`, `:1.3`, `:latest`), multi-arch (amd64 + arm64).

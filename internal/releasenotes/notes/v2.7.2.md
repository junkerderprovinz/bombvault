A nicer Run History on the dashboard, from @manilx's feedback. Thanks.

## Added
- **Run History is scrollable and filterable by day.** The dashboard's Run History now shows the full recent window (up to the last 500 runs) in a scrollable list instead of just the latest handful, with a **Day** dropdown to jump to a specific day's backups. Hover a row's time to see the exact timestamp.

Image: `ghcr.io/junkerderprovinz/bombvault:2.7.2` (also `:2`, `:2.7`, `:latest`) and `docker.io/junkerderprovinz/bombvault:2.7.2`, multi-arch (amd64 + arm64).

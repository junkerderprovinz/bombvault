One bug fix: the file-set restore-to-folder target now defaults correctly.

## Fixed
- **File-set "restore to a folder" now pre-fills your real default (#69).** The whole-set and selective restore pickers under the Files tab showed only a generic placeholder example ("user/bombvault/container") instead of your configured "Default restore folder" from Paths & Storage — exactly what the container restore panel already did correctly. Both pickers now seed from the same setting, and a startup race that could occasionally leave either one on the fallback value is closed too.
